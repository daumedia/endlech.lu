# Befunde — projektweit

Stand: 2026-09-11 · Quelle: die `qa-report.md` aller geprüften Features

Diese Liste wird von `sdd-qa` fortgeschrieben, nicht von Hand. Sie ist die Grundlage
des Auditberichts, den `/sdd-erfassen abschluss` daraus baut.

**Geprüft bisher:** B01 (17/20), B02 (16/17), B03 (16/20), B04 (23/24 im zweiten
Durchlauf), B23 (34/35 im zweiten Durchlauf), B19 (17/17, davon eines nicht prüfbar),
B14 (28/28), B15 (27/27), B22 (30/30), B17 (25/25) — abgenommen.
B10 (24/24 im zweiten Durchlauf), B18 (29/29), B11 (18/19, eines nicht prüfbar), B20 (19/20), B21 (20/20), B09 (18/18), B05 (24/24), **B06 (23/23 — das erste Feature ohne eigenen Befund)**, B07 (17/17, eines nicht ausgeführt), B08 (16/16) — abgenommen.
B12 (15/15 nach der Reparatur), B13 (14/14), B16 (29/29), B24 (16/16) — abgenommen.

**2026-08-29 · Feature `04`, zweiter Durchlauf: 42/48 bestanden.** Fünf der sechs
Befunde sind behoben und gegengeprüft, darunter der schwerste (BF-84). **BF-83 war nur
zur Hälfte behoben** — fortgeführt als BF-89 (kritisch), unabhängig von Messung und
`code-reviewer` bestätigt. **BF-89 wurde am selben Tag behoben**, diesmal an der Ursache:
`selfConfirmedAt` trennt den eingelösten Double-Opt-In vom Verwaltungs-Backfill. Offen
bleiben BF-88 (Betreiberentscheidung) und BF-90 (niedrig).

**2026-08-30 · Feature `04`, vierter Durchlauf: 43/48 — der erste ohne neuen Befund.**
BF-91 und BF-92 behoben, **AK-08 hält wieder**, 681 Tests grün. Geprüft wurde diesmal die
**vollständige Zustandsmatrix** von `confirm()` über alle sechs Ausgangszustände und
**beide** Wartelisten — die ersten drei Durchläufe hatten fast alles nur am Partner-Weg
gemessen. Offen bleiben allein BF-88 und BF-90, beide ohne Softwareanteil. Der Code ist
auslieferbar; die **Inbetriebnahme** hängt an T08 (Brevo-Konto) und BF-88.

**2026-08-29 · Feature `04`, dritter Durchlauf: 43/48 bestanden.** BF-89 ist behoben —
**AK-05 hält erstmals auf allen drei Wegen**. Die Reparatur führte aber **BF-91** ein
(hoch): Der Bestätigungsklick setzt einen fortgeschrittenen Vertriebsstatus zurück, weil
`confirm()` jetzt einen Pfad erreicht, der vorher nie offen war — und der Rückfall wandert
bis nach Brevo, womit **AK-08 durchfällt**. Dritte Runde an derselben Stelle. Dazu BF-92
(niedrig): `docs/data-model.md` führt Feature 04 überhaupt nicht. **Beide wurden am
2026-08-30 behoben** — die vierte Abnahme steht aus.

**2026-08-29 · Feature `04` geprüft (erster Durchlauf): 41/48 bestanden, 3 durchgefallen, 4 nicht prüfbar.**
Nicht abgenommen — zwei kritische Befunde (BF-83, BF-84) und einer hoher (BF-86). Die
vier nicht prüfbaren Kriterien hängen alle am selben Punkt: Das Brevo-Konto ist nicht
eingerichtet (AK-07, AK-10, AK-24, AK-27) — ohne Kontozugang lässt sich weder messen, ob
die Attribute ankommen, noch die Kontaktzahl gegenprüfen.

**2026-08-25 · Alle 72 Befunde sind behoben.** Der Durchgang lief über zehn
Blöcke; die Reihenfolge folgte dem Schweregrad, nicht der Feature-Nummer. Dabei
sind vier Muster geschlossen worden, die je mehrere Features betrafen:

| Muster | Vorkommen | Antwort |
|---|---|---|
| M-01 · Ungedeckelte Wege | 7 | `ActionLimiter` plus sechs neue Limiter; `LimiterCoverageTest` prüft die Konvention |
| Fehlende Eingabeprüfung → 500 | 4 | `empty_data`, Längen- und Slug-Kürzung |
| Fehlende Übersetzungen | 2 | Scanner über 736 Template-Schlüssel und 187 Formularangaben |
| Betroffenenrechte | 3 | Feature `01` |

Zwei davon fielen erst beim Reparieren auf: ein zweiter 500er im
Vorschlags-Assistenten (beim Messen des Rate-Limits) und eine Lücke im eigenen
Katalog-Scanner, der Beschriftungen nicht erfasste.

**Alle Reparaturen liegen auf `dev` bzw. `fix/befunde-abarbeiten` und sind noch
nicht auf `production`.**

**Zuordnung von Befunden:** Ein Befund steht bei dem Feature, in dem er **behoben** wird
— nicht bei dem, in dem er gefunden wurde. BF-04 wurde in B01 gefunden und ist seit
2026-08-23 dem Feature `01` zugeordnet; sonst hielte er B01 dauerhaft auf `review`,
obwohl dort nichts mehr zu reparieren ist.

> Nicht zu verwechseln mit `features/fehlbestand-uebersicht.md`. Jene sammelt, was beim
> **Lesen** des Codes auffiel, und ist eine Suchliste. Hier stehen nur Befunde, die in
> einer QA **ausgeführt und belegt** wurden.

**2026-08-30 · Feature `05` geprüft (erster Durchlauf): 32/44 bestanden, 5 durchgefallen,
7 nicht prüfbar.** Nicht abgenommen — zwei Befunde mit Grad *hoch*. **Alle zwölf offenen
Kriterien hängen an den drei Vorbedingungen der Spec** (Vektormarken, Postfach,
Betreiberangaben); nur einer der vier Befunde hat überhaupt einen Codeanteil (BF-95). Der
Angriffsdurchlauf blieb ohne Fund, axe-core meldet in allen vier Sprachfassungen null
Verstöße.

⚠ **Der 768-px-Überlauf war bereits als BF-80 erfasst.** Er wurde beim Selbsttest von `05`
erneut gefunden und dort zunächst als neuer Befund geführt — falsch. Die Messung ist
trotzdem ein Gewinn: Sie zeigt, dass der Fehler **angemeldet doppelt so groß ist und bis
unter 1000 px reicht**. BF-80 ist entsprechend ergänzt statt verdoppelt.

**2026-08-30 · Feature `05`, zweiter Durchlauf: 37/44, kein neuer Befund.** Beide
Reparaturen halten der Gegenprobe stand. Der Ertrag liegt in der Methode: Statt auf VB-01
zu warten, wurde der **vollständige Zustand hergestellt** (SVG-Platzhalter,
`app:press:package`) — damit lief die gesamte Materialmechanik zum ersten Mal durch und
**sechs vormals offene Kriterien sind belegt**. In diesem Zustand meldete die Suite 10
statt 13 übersprungene Tests. Alle Prüf-Artefakte wurden wieder entfernt. Offen bleiben
BF-93 bis BF-96 — **keiner davon hat einen Softwareanteil**.

**2026-08-30 · Feature `05`, dritter Durchlauf: 42/44 — production-ready.** Die drei
Vorbedingungen sind erfüllt; **fünf Kriterien wechselten von offen auf bestanden**, und
**kein Prüflauf dieses Features überspringt mehr**. BF-93, BF-94 und BF-96 sind behoben.
Neu ist allein **BF-99** (mittel, kein Codeanteil): Der Schriftzug der Wort-Bildmarken ist
noch nicht in Pfade umgewandelt.

⚠ **AK-11 bleibt durchgefallen, und das ist eine Entscheidung** (OF-04): Es wird keine
Anschrift veröffentlicht. Das Impressum zitiert weiterhin § 5 TMG / Art. 11 Loi sur le
commerce électronique und erfüllt beides nicht. Das Feature hat den Zustand sichtbar
gemacht, nicht verursacht — vorher stand dort gar nichts außer dem Namen der Plattform.
Ob AK-11 gestrichen oder zurückgenommen wird, entscheidet Michael in der Spec.

Der Angriffsdurchlauf blieb zum dritten Mal ohne Fund, diesmal einschließlich der neuen
Angriffsfläche: **vier SVG-Dateien von derselben Herkunft** — kein `<script>`, kein
`onload`, kein `foreignObject`, kein nachgeladenes Bild.

**2026-08-30 · Nach dem Deploy von v2026.08.30 auf Produktion gefunden: BF-100 (kritisch).**
Der sprachfreie Kurzlink `/presse` läuft in eine Weiterleitungsschleife, weil das neue
Verzeichnis `public/presse/` denselben Namen trägt wie die Route. Alles andere des
Releases ist auf Produktion belegt — Paket, Marken, Faktenblatt mit den echten Zahlen
(3/3/2, identisch mit `/open.json`), Fußzeilen-Adresse, Impressum mit Betreiber, keine
Testdaten, 404 ohne Stacktrace, Fußzeile zeigt `v2026.08.30`.

⚠ **Ein Muster, das über dieses Feature hinausgeht:** Ein Verzeichnis unter `public/`
darf nicht so heißen wie eine Route. Der Entwicklungsserver zeigt die Kollision nicht —
er hat kein `mod_dir`. Wer eine sprachfreie Kurzroute anlegt und gleichnamige Dateien
ausliefert, hat sie gebaut, ohne es zu merken.

## Offen

| ID | Feature | Befund | Grad | Fundstelle | Status |
|---|---|---|---|---|---|
| BF-147 | 10 | **Die Seitenzahl-Regel gilt für jede Seite, nicht nur für blätternde Listen.** `design.md` (Entscheidung 11) begrenzt „nur `page` als ganze Zahl ab 2 bleibt im canonical-Verweis“ auf die Restaurantliste; umgesetzt ist sie für jede Route. Gemessen im Produktionsmodus: `/de/about?page=2` → canonical `https://endlech.lu/de/about?page=2`, ebenso `/de/presse?page=12`, `/lb/vergleich/wheelmap?page=3`, `/de/restaurants/1?page=7` — und in allen Sprachverweisen. Inhaltlich ist die Variante `/de/about` (Unterschied nur im Sprachumschalter, BF-68). **Folge:** Jede verlinkte `?page=N`-Adresse ist eine Dublette, die sich selbst kanonisiert — genau das, was der Verweis verhindern soll. Dazu liefert das Board auf `?page=99` eine leere Seite mit 200, die sich selbst für maßgeblich erklärt. Kein Sicherheitsproblem: Die Seitenzahl kommt als geprüfte ganze Zahl heraus. ⚠ **Warum der Bau es nicht sah:** `SeoUrlBuilderTest` und `SeoHeadTest` prüfen die Regel nur an der Restaurantliste — am Fall, für den sie entworfen wurde. Der `code-reviewer` las den Code, hielt die generische Regel wegen des ebenfalls blätternden Boards für harmlos und übersah die nicht blätternden Seiten; gefunden hat es der Abruf von außen. | mittel | `src/Seo/SeoUrlBuilder.php` (`retainedQuery()` für jede Route), `src/Twig/SeoExtension.php`; Reproduktion `Qa10SitemapBestandTest::testBf147SeitenzahlNurAufSeitenDieBlaettern` (übersprungen), Belege `qa/10/server-belege.md` | **behoben 2026-09-13**, ausgeliefert mit `v2026.09.13` — die Seitenzahl bleibt nur auf blätternden Seiten (`SeoRegistry::PAGINATED_ROUTES`, Abgleich in `SeoRouteCoverageTest`). Nachgeprüft lokal (384 Abrufe, Gegenprobe 740) und auf der Produktion (288 Abrufe ohne Abweichung, `qa/10/produktion-2026-09-13.md`) |
| BF-148 | 10 | **EC-05 beschreibt ein Verhalten, das die Restaurantliste nicht hat.** Die Spec sagt, eine Seitenzahl jenseits der letzten Seite nenne sich selbst als maßgeblich; gemessen antwortet die Liste mit **404** und nennt nichts (`?page=3` und `?page=99` bei 11 Restaurants). Harmloser als beschrieben. ⚠ Dieselbe Ursache wie OF-06: Die Spec nahm an, die Liste liefere für jede Seitenangabe eine Seite — bei `?page=abc` antwortet sie mit 400. | niedrig | `features/10-sitemap-robots/spec.md`, EC-05 | offen — als OF-08 in der Spec an den Betreiber, `sdd-build` ändert die Spec nicht |
| BF-149 | 11 | **Ein hängender Zähl-Eingang hält gleichzeitige Anfragen bis 3,7 s fest, und eine Seite wartet mit.** Der Unterbrecher wird erst **nach** einem gescheiterten Aufruf gesetzt; bis dahin startet jede parallele Anfrage an `/api/send` ihren eigenen Versuch mit vollem Zeitlimit (2 s, höchstens 3 s) und belegt einen PHP-Prozess. Gemessen gegen einen Eingang, der Verbindungen annimmt und nie antwortet: einzeln 2,12 s, danach 0,08 s; **sechs gleichzeitig je 2,19–3,67 s**, `/de/restaurants` im selben Fenster **2,04 s** bis zum ersten Byte statt 0,10 s. Wiederholt sich alle 60 s, sobald der Unterbrecher abläuft. AK-37 hält im Browsermittel (+67 bis +73 ms), einzelne Startseiten lagen bei 540–576 ms. Wie stark es in Produktion wirkt, hängt an der Threadzahl von FrankenPHP; die Ursache nicht. ⚠ **Warum der Bau es nicht sah:** `UmamiForwarderTest::testUnterbrecherGreiftNachFehlschlagUndLoestSich` prüft die Folge von Aufrufen, nicht ihre Gleichzeitigkeit — und ein Hänger ist etwas anderes als ein Ausfall (dieselbe Lehre wie beim Nahverkehr: der `catch` fängt den Ausfall, nicht die Verzögerung). | mittel | `src/Usage/UmamiForwarder.php:81–84`, `:111–116`; Belege `qa/11/ladezeit.ausgabe.txt` (Nachmessung, paarweise Kontrolle) | **behoben 2026-09-13** — höchstens eine Weiterleitung zur Zeit (Sperre `umami-weiterleitung`, 100 ms Wartezeit auf den Platz). Reproduktion vorher/nachher: sechs Zählaufrufe je 2,05–3,88 s → einer 2,06 s, fünf 0,17–0,40 s; Restaurantliste 1,8 s → 0,06 s (`qa/11/bf149-nachpruefung.ausgabe.txt`). ⚠ Preis gemessen: von sechs exakt gleichzeitigen Aufrufen an einen gesunden Eingang gehen 0–1 verloren. **Von der QA nachgeprüft 2026-09-13:** nach echtem Ablauf des Unterbrechers genau ein Wartender (2,10 s), im FrankenPHP-Image trennt die Sperre die Threads eines Prozesses (`qa/11/haenger.ausgabe.txt`, `qa/11/frankenphp-sperre.ausgabe.txt`); Folgebefund BF-152. Noch nicht ausgeliefert |
| BF-150 | 11 | **Kein Trichter zählt: `*` mitten im Pfad versteht Umami nicht.** `growth/config.json` und `design.md` definieren die Schritte als `/*/restaurants`, `/*/restaurants/*`, `/*/app`, `/*/partner`, `/*/organisationen*`. Umami 3.3.1 ersetzt den Stern nur, wenn der Schritt damit beginnt oder endet (`getFunnel.ts`: `startsWith('*') \|\| endsWith('*')`, `replace(/^\*\|\*$/g, '%')`). Gemessen gegen eine echte Umami-Instanz auf denselben Daten: ausgelieferte Kette **0 · 0 · 0 · 0**, mit führendem Stern (`*/restaurants` …) **1 · 1 · 1 · 1**; `/*/app` 0 · 0 gegen `*/app` 1 · 1. AK-14 und AK-15 nicht erfüllt, AK-35 trägt eine wirkungslose Schrittfolge. **Folge:** Der Growth-Loop meldete jede Woche Trichter mit null Besuchern — das liest sich als „niemand sucht" und heißt „falsch abgefragt". ⚠ **Warum der Bau es nicht sah:** `design.md` begründet die Form mit `getFunnel.test.ts` (`'*/thanks'` → `'%/thanks'`) — ein Beispiel mit **führendem** Stern, das die Behauptung „auch mitten im Pfad" gar nicht trägt. Das Skill-README übernahm den Satz, der MCP-Server reicht den Wert unverändert durch, und der `code-reviewer` hatte keine Instanz; gefunden hat es die Abfrage gegen Umami. | hoch | `growth/config.json:22,24,39,43,47`; `features/11-nutzungsmessung/design.md:107–116`; `tasks.md:86`; außerhalb des Repositorys `~/.claude/skills/growth-loop/mcp-umami/README.md:64–66`; Reproduktion `Qa11TrichterMusterTest::testBf150KeinPlatzhalterMittenImPfad` (übersprungen), Beleg `qa/11/trichter.ausgabe.txt` | **behoben 2026-09-13** — `growth/config.json` mit führendem Stern, Skill-README und `einrichtung.md` berichtigt. Nachgeprüft gegen eine frische Umami-3.3.1-Instanz: Suche 2·2·2·2, Wartelisten 1·1 / 1·1 / 2·2, alte Schreibweise weiterhin 0 (`qa/11/bf150-nachpruefung.ausgabe.txt`); `GrowthTrichterTest` hält die Schritte gegen die Routen aller Sprachen. ⚠ `design.md` bleibt falsch bis OF-07. ⚠ Die README sagte vor T28 richtig „am Anfang oder Ende" — der Bau hatte eine korrekte Aussage überschrieben. **Von der QA nachgeprüft 2026-09-13** auf Daten, die durch die Anwendung liefen: Suche 1·1·1·1, App 1·1, Partner 1·1, Organisationen 3·1 (`qa/11/trichter-nachpruefung.ausgabe.txt`, `qa/11/wartelisten-browser.ausgabe.txt`). Noch nicht ausgeliefert |
| BF-151 | B15 | **Eintragen von den Zielgruppenseiten endet in einer 405-Fehlerseite — auf der Produktion aktiv.** Auf `/{locale}/organisationen/gemeinden`, `…/unternehmen` und `…/vereine` trägt das Formular kein `action`; der Browser schickt an die Zielgruppenseite, die nur GET kennt. Gemessen im Browser: **405**, „Oops! An Error Occurred", **0** Einträge, eingegebene Daten verloren — auf allen drei Seiten. Produktion lesend geprüft: dasselbe Formular ohne `action`, dieselben Routen auf `master`. Sentry sieht es nicht (405 ignoriert). ⚠ Seit Feature 10 stehen die drei Seiten in der Sitemap. ⚠ **Warum drei QA-Durchläufe es nicht sahen:** Jeder Absende-Test holt das Formular von der Übersicht, wo die aktuelle Adresse zufällig die POST-Route ist. Gefunden beim Prüfen von Feature 11 (AK-15), weil der Trichter der Organisationen dort keine Eintragung bekam. | hoch | `templates/organisation/_form.html.twig:33`, `OrganisationController::type()`; Reproduktion `Qa11ZielgruppenFormularTest` (3 Fälle, übersprungen), Beleg `qa/11/wartelisten-browser.ausgabe.txt` | offen — B15 auf `review` |
| BF-152 | 11 | **Eine defekte Sperre macht aus dem Zählaufruf einen Serverfehler.** Die Sperre aus BF-149 kam ohne die Fehlerbehandlung, die der Rest der Weiterleitung hat: `Lock::acquire()` fängt nur den Konflikt ab und wirft jede andere Ausnahme weiter, `forward()` und `CollectController` fangen sie nicht. Am laufenden Server: Sperrdatei auf `chmod 000` → **500** mit HTML-Fehlerseite statt 202; Rechte zurück → 200. Tritt ein bei vollem oder schreibgeschütztem Temp-Verzeichnis oder fremdem Eigentümer der Sperrdatei. Folge: jeder Zählaufruf ein 500er, und Sentry meldet jeden einzelnen (500 steht nicht in `ignore_exceptions`). ⚠ Vom `code-reviewer` gefunden, von der QA verifiziert; die Reparatur-Prüfläufe des Baus nutzten nur einen Speicher, der ausschließlich den Konflikt wirft. | mittel | `src/Usage/UmamiForwarder.php`, `platzBelegen()` und `release()` im `finally`; Reproduktion `Qa11SperrAusfallTest` (jetzt scharf), Beleg `qa/11/sperre-ausfall.ausgabe.txt` | **behoben 2026-09-13** — Belegen im `try` (Klasse loggen, Unterbrecher setzen, 202), Freigeben mit eigenem `catch`, Sperre ohne automatische Freigabe. Am laufenden Server: `chmod 000` → 202 statt 500 (`qa/11/sperre-ausfall-nachpruefung.ausgabe.txt`); vier Gegenproben rot. **Von der QA nachgeprüft 2026-09-14** an der laufenden Anwendung: gesperrte Sperrdatei → 202, danach wird wieder gezählt; ohne automatische Freigabe bleibt kein Platz hängen (20 + 10 + 5 + 5 Aufrufe nacheinander angekommen), der eine Platz aus BF-149 hält (`qa/11/bf152-qa.ausgabe.txt`). Noch nicht ausgeliefert |
| BF-140 | B25 | **Profilbilder landen im Cache des Service Workers.** Der letzte `fetch`-Zweig nimmt jede Antwort mit `destination === 'image'` auf; Avatare liegen unter `/uploads/avatars/` und erfüllen die Bedingung. Gemessen: `avatar_im_cache=["https://endlech.lu/uploads/avatars/nutzer-7.jpg"]`. ⚠ AK-19 und AK-20 schliessen genau das aus („keine personenbezogenen Inhalte"). **Folge:** Auf einem geteilten Gerät bleibt das Profilbild eines abgemeldeten Nutzers liegen — bis `CACHE_VERSION` wechselt, also möglicherweise monatelang. Kein Abfluss an Dritte, aber ein personenbezogenes Datum ohne Zweck (Art. 5 Abs. 1 lit. e DSGVO). ⚠ Restaurantfotos (`/uploads/restaurants/`) dürfen bleiben — sie sind Gemeingut der Plattform; die Unterscheidung ist der Punkt. | ~~mittel~~ **niedrig** | `public/sw.js:96-104`; Nachweis `qa/B25/sw-verhalten.mjs`, Abschnitt *Angriff 2* | **behoben 2026-09-12** — `public/sw.js` cacht Bilder jetzt über eine **Positivliste** (`/icons/`, `/images/`, `/uploads/restaurants/`, `/uploads/team/`) statt über `destination === 'image'`. Gegenprobe: `avatar_im_cache=nein`, `restaurantfoto_im_cache=JA`, `portraet_im_cache=JA`, `app-icon_im_cache=JA` — und **`fremder-uploads-pfad_im_cache=nein`**, das ist der eigentliche Gewinn: Eine Ausnahme für `/uploads/avatars/` hätte den heutigen Fall behoben und den nächsten personenbezogenen Pfad wieder mitgenommen (Prinzip wie `@source` in `app.css`). Rückbau auf die alte Bedingung färbt alle fünf Fälle auf `JA`. ⚠ **`CACHE_VERSION` auf `endlech-v3` ist Teil der Reparatur**, nicht Zierde: Die Liste verhindert neue Einträge, die **bereits gecachten** Bilder verschwinden erst, wenn `activate` den alten Cache löscht. ⚠ **Grad von *mittel* auf *niedrig* berichtigt, nach Klärung am selben Tag:** Der Angriff hiess `nutzer-7.jpg` und suggerierte fremde, erratbare Avatare — beides trifft nicht zu. Gerendert wird ausschliesslich `app.user` (Kopfzeile, Profilseite), und die Dateinamen entstehen aus `uniqid('', true)`, sind also nicht ableitbar. Es geht um das **eigene** Bild im **eigenen** Browser; der Zugriffsweg ist ein geteiltes Gerät plus Inspektion der Cache-API. AK-20 bleibt verletzt (die Zusage ist absolut formuliert), der Schaden ist kleiner als beim Fund angenommen. Noch nicht ausgeliefert |
| BF-141 | B25 | **Der Worker greift auf dem locale-präfixierten API-Weg ein.** `/de/api/cuisines/search` landet im cache-first-Zweig, weil `startsWith('/api/')` dort nicht zutrifft — der ältere `CuisineApiController` liegt weiterhin unter `/{_locale}`, anders als `/api/v1` und `/open`. Gemessen: `locale_api_eingegriffen=JA`. **Heute kein Schaden**, weil dieser Zweig nur Bilder und `/icons/` aufnimmt; wer ihn je erweitert, macht die Lücke im selben Moment scharf. | niedrig | `public/sw.js:58-61`; Nachweis `qa/B25/sw-verhalten.mjs`, *Angriff 1* | **behoben 2026-09-12** — die Ausnahme erlaubt jetzt **ein** Sprachsegment vor `/api/` (`/^\/(?:[a-z]{2}\/)?api\//`). Gegenprobe: `/api/v1 → nein`, `/de/api/cuisines → nein`, `/lb/api/cuisines → nein`; Rückbau auf `startsWith('/api/')` färbt den locale-Weg wieder auf `JA`. ⚠ `/open.json` greift der Worker weiterhin ab — das ist richtig: Der Endpunkt trägt ausdrücklich `public, max-age=3600`, Caching ist dort gewollt. Noch nicht ausgeliefert |
| BF-142 | B25 | **AK-17 und FB-01 beschreiben einen überholten Stand.** Die Spec sagt, auf dem Telefon gebe es keinen Weg zum Abmelden und zur Sprachwahl, und nennt das „den zentralen Befund" des Features. Gemessen bei 500 px: Abmelden sichtbar (**387 × 44 px**, das zweite Logout-Formular der Profilseite trägt kein `hidden md:`), Sprachwahl sichtbar (55 × 28 px, seit BF-72), „Vorschlagen", Partner und Organisationen in der Fussleiste, „Über uns" in der Leiste und seit dem 2026-09-12 auch in der Fussleiste. Was tatsächlich fehlt, ist der Zugang zum Menü „Mitmachen". ⚠ Vierte Ausprägung des Musters nach BF-126, BF-130 und BF-132: Die Rekonstruktion veraltet mit jeder Reparatur, die sie behebt — hier mit BF-72 und mit dem Deploy vom 2026-09-12. | niedrig | `features/B25-pwa-mobile-navigation/spec.md:88-97` (AK-17) und `:120-121` (FB-01) | **behoben 2026-09-12** — AK-17 trägt jetzt den gemessenen Stand samt Zahlen (Abmelden 387 × 44 px auf der Profilseite, Sprachwahl 55 × 44 px im Kopf), FB-01 nennt die drei geschlossenen und die eine offene Lücke, und OF-01 sagt, dass die Frage kleiner geworden ist: Ein Menüfeld träge nur noch „Mitmachen" und kostete dafür „Über uns". ⚠ Der alte Wortlaut bleibt durchgestrichen stehen — wer die frühere Einschätzung „der zentrale Befund" kennt, soll sehen, was daraus geworden ist. Noch nicht ausgeliefert |
| BF-143 | B25 | **64 px Leerraum am Fuss jeder Verwaltungsseite auf dem Telefon.** `<main>` trägt `pb-16 md:pb-0` unabhängig von der Route; die Leiste erscheint im Verwaltungsbereich aber bewusst nicht (AK-13). Gemessen auf `/de/admin` bei 500 px: `bottomnav=FEHLT` **und** `main.paddingBottom=64px`. Rein kosmetisch. | niedrig | `templates/base.html.twig`, `<main class="flex-grow pb-16 md:pb-0 …">` | **behoben 2026-09-12** — das Polster hängt an derselben Bedingung wie die Leiste (`_route starts with 'admin_'`). Gemessen: `/de/admin` bei 500 px → `main.paddingBottom=0px` (vorher 64), Gegenprobe `/de/` → weiterhin 64. `PwaTest::testBf143PolsterNurWoDieLeisteLiegt` prüft beide Richtungen. Noch nicht ausgeliefert |
| BF-144 | B25 | **Der Sprachumschalter ist auf dem Telefon 28 px hoch.** Gemessen: 55 × 28 px. WCAG 2.5.8 (24 × 24) ist erfüllt, die Projektregel `min-h-[48px]` für Aktionen nicht — und seit BF-72 ist der Umschalter ausdrücklich für Mobil gedacht. Damit ist er das kleinste Ziel im mobilen Kopfbereich. | niedrig | `templates/partials/_language_switcher.html.twig`, Knopf mit `px-2 py-1` | **behoben 2026-09-12** — `min-h-[44px]` und `py-2.5` unter `md`, ab `md` bleibt die kompakte Form (dort ist der Platz knapp, BF-80). Gemessen: **55 × 44 px** statt 55 × 28. ⚠ Gegengeprüft, dass die Kopfzeile dadurch nicht wieder überläuft: 0 px bei 768 und 1024 px in beiden geprüften Sprachen. Noch nicht ausgeliefert |
| BF-145 | B25 | **`offline.html` trägt `lang="de"`, der Inhalt ist luxemburgisch.** Sichtbar stehen dort „Keng Internetverbindung“, „Du bass momentan offline. Iwwerpréif deng Verbindung a probéier et nach eemol.“ und „Nach eemol probéieren“ — am `lb`-Katalog gegengeprüft, im `de`-Katalog gibt es keine Entsprechung. Ein Screenreader wendet damit deutsche Ausspracheregeln auf luxemburgischen Text an (WCAG 3.1.1). ⚠ **Nicht der Text ist falsch, sondern die Auszeichnung:** `translation.yaml` setzt `default_locale: lb`, und eine statische Datei ist nicht lokalisierbar (EC-01) — Luxemburgisch ist hier richtig, `lang="lb"` wäre die passende Angabe. Vom `code-reviewer` gemeldet, verifiziert. | niedrig | `public/offline.html:2` | **behoben 2026-09-12** — `<html lang="lb">`, mit Begründung als Kommentar in der Datei. `PwaTest::testBf145OfflineSeiteIstAlsLuxemburgischAusgezeichnet` prüft das Attribut **und** den luxemburgischen Text: Wer den Text übersetzt, wird dadurch an das Attribut erinnert. Noch nicht ausgeliefert |
| BF-146 | B26 | **Die Spezifikation beschreibt an zwei Stellen einen überholten Stand.** (1) **FB-05** sagt „Keine Fokusführung. Beim Erscheinen wandert der Fokus nicht ins Banner" — gemessen wandert er beim Klick auf „Cookie-Einstellungen" sehr wohl (`nach reopen: fokus=DIV [im Banner]`, Banner trägt `tabindex="-1"`); beim **automatischen** Erscheinen bleibt er aussen, und das ist im Controller begründet: Sonst zöge der Fokus den ersten Tab ins Banner und der Skip-Link wäre nicht mehr das erste Tab-Ziel (BF-74, WCAG 2.4.1 — gemessen ist er es: „Zum Inhalt springen"). ⚠ **OF-02 vermerkt das bereits** („Geprüft 2026-08-25: Bereits umgesetzt"), nur wurde FB-05 nicht nachgezogen — die Spec widerspricht sich selbst. (2) **AK-11** führt in einer Klammer „Der Feedback-Link in der Fußzeile führt zu `endlech.userjot.com`" — der Link zeigt seit dem 2026-08-30 aufs eigene Board, und userjot ist seit dem 2026-09-12 abgeschaltet (BF-103). ⚠ **Die Folge liegt in der Zukunft:** FB-05 ist der Punkt, an dem jemand eine Fokusführung „nachrüstet", die absichtlich so ist — und dabei BF-74 wieder aufreisst. | niedrig | `features/B26-cookie-banner/spec.md`, FB-05 und die Klammer in AK-11 | **behoben 2026-09-12** — beide Stellen berichtigt. FB-05 sagt jetzt, dass die Fokusführung beim nutzergetriggerten Öffnen greift (gemessen: `fokus=DIV [im Banner]`) und beim automatischen Erscheinen bewusst nicht, mit Verweis auf BF-74 und den Skip-Link; die Klammer in AK-11 nennt statt `endlech.userjot.com` das eigene Board und hält fest, dass die Zusage „keine Fremdressourcen" auch nach BF-99 gilt (Inter liegt unter `/build/fonts/`, gemessen `fremde_ressourcen=[]`). ⚠ Die Spec widersprach sich selbst: OF-02 vermerkte die Umsetzung seit dem 2026-08-25, FB-05 behauptete das Gegenteil. Noch nicht ausgeliefert |
| BF-139 | 04 | **Der dokumentierte Auftragsverarbeitungsvertrag mit Brevo beschreibt einen überholten Vertragsstand.** `docs/datenschutz.md` führte ihn als „Annex 2 der General Terms and Conditions“, Stand 15.05.2024, mit Löschfrist **100 Tage von selbst** (Ziff. 8.1), Löschbescheinigung (8.2), Vorrang (1.4) und 72-Stunden-Meldefrist. Jede dieser Angaben war **korrekt wiedergegeben** — die PDF-Kopie belegt sie Klausel für Klausel —, aber Brevo hat das Dokument seither **neu gefasst**: Es ist jetzt **Appendix 3** der Terms of Service, die Nummerierung ist anders, und „Annex 2“ bezeichnet dort die **Liste der Unterauftragsverarbeiter**. Wer der alten Fundstelle folgt, findet das falsche Dokument. ⚠ **Zwei inhaltliche Änderungen, eine davon mit Handlungsbedarf:** Die Löschung am Vertragsende erfolgt nur noch **auf Verlangen** und binnen drei Monaten (Ziff. 5.3) — wer kündigt und nichts verlangt, dessen Daten bleiben liegen; und die **72-Stunden-Frist** für die Meldung einer Datenschutzverletzung ist durch „without undue delay“ ersetzt (Ziff. 5.7). ⚠ **Die Liste der Unterauftragsverarbeiter hat sich verschoben:** Datadog, Scaleway und Hetzner sind heraus, Omni (Dashboards, USA) ist in die **Infrastrukturgruppe** gerückt und damit nicht abwählbar, und vier **KI-Anbieter** sind neu (OpenAI, Google Gemini, Anthropic, Langfuse — nur bei Nutzung der jeweiligen Funktion). ⚠ **Das schlug bis in die öffentliche Erklärung durch:** `legal.transfer_text` sagte in vier Sprachen „betrifft Protokollierung und Ausfallschutz, **nicht die Inhalte**, die Sie uns anvertrauen“ — Datadog (die Protokollierung) ist weg, und für Omni liesse sich diese Zusage nicht belegen. | mittel | `docs/datenschutz.md` (Brevo-Abschnitt, Unterauftragsverarbeiter, Checkliste), `docs/anfragen/2026-09-brevo-dpa.md`, `translations/messages.*.yaml` (`legal.transfer_text`), `templates/impressum/index.html.twig` | **behoben 2026-09-12** — alle Angaben gegen den Online-Stand abgeglichen und nachgezogen, mit Abgleichstabelle (alte gegen neue Klausel) statt stiller Überschreibung. Die geltende Fassung ist als PDF gesichert (`qa/brevo-dpa/BREVO-Appendix-3-DPA-abgerufen-2026-09-12.pdf`), die alte bleibt daneben liegen. Der öffentliche Satz nennt jetzt „technische Dienste: Zustellung und Ausfallschutz, Auswertung und Nutzerunterstützung“ und lässt die unbelegbare Zusage weg — **eine Zusage, die man nicht belegen kann, gehört nicht in eine Datenschutzerklärung**. ⚠ **DS-01b ist entfallen** (die Anmeldung zum Benachrichtigungsformular verlangt der Vertrag nicht mehr; Ziff. 7.3 informiert in jedem Fall, vor der Freigabe, mit 30 Tagen Kündigungsrecht) — die vorbereitete Anfrage an `dpo@brevo.com` ist als überholt gekennzeichnet statt gelöscht. **DS-01c erledigt**, DS-01d (Datum der AGB-Annahme) bleibt offen. ⚠ **Was bleibt, ist eine Handlung: Die Löschung am Vertragsende gehört in die Kündigungsschritte** — sie geschieht nicht mehr von selbst. Neu als **DS-01e** vermerkt: Fassungsprüfung einmal im Jahr, denn ein Vertragsstand veraltet lautlos. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-138 | 01 | **Der Passwort-Reset scheitert mit HTTP 500, wenn die gespeicherte Adresse RFC 2822 verletzt.** Gemessen: RFC-widriger Altbestand → **500**, unbekannte Adresse → 302, gültige Adresse → 302. Ausnahme ist `Mime\Exception\RfcComplianceException`; `sendeLink()` fängt nur `TransportExceptionInterface`, und der Wurf passiert **vor** `gleicheLaufzeitAn()` — der BF-137-Angleich wird damit übersprungen. ⚠ Zwei Schäden: Enumeration über den Statuscode (stärker als die 12 ms aus BF-137) **und Zugangsverlust** — für ein betroffenes Konto ist der Reset vollständig kaputt, also genau der Weg, der FB-05 aus B01 schließen sollte. ⚠ **Grad hängt am Bestand:** Seit BF-119 entstehen solche Adressen nicht mehr neu; ob auf Produktion noch welche liegen, ist von hier aus nicht feststellbar. Der Nachweis ist ein Prüfbefehl im Bericht — ⚠ **keine SQL-Heuristik**: Das erste Muster dort übersah **sechs von sieben** Verstößen, maßgeblich ist `Mime\Address` selbst. Der Befehl ist lokal in beide Richtungen validiert (7 eingesetzte Adressen → 7 Treffer, Fixture-Stand → 0). **Am 2026-09-12 auf Produktion ausgeführt: 0 betroffene Konten** — der Befund bleibt damit *mittel*, kein Nutzer ist heute ohne Rückweg. ⚠ Das ist ein Stichtag, keine Zusage: Ein Bestandsimport oder ein zweiter Schreibweg auf `user.email` kann ihn entwerten, und der Zweig bleibt bis zur Reparatur ungeschützt. ⚠ `PasswordResetRequestType` nutzt weiterhin den nicht-strikten `Email`-Constraint — schließt die Lücke aber nicht, weil an die **gespeicherte** Adresse versendet wird. Vom `code-reviewer` gefunden, am Server nachgestellt | mittel | `src/Controller/PasswordResetController.php` (`sendeLink()` und der Block davor) | **behoben 2026-09-12** — die Adresse wird als `Mime\Address` geprüft, **bevor** ein Token entsteht (Muster aus BF-119); wirft sie, greift ein `catch (RfcComplianceException)` und der Ablauf endet wie im unbekannten Zweig. Reproduziert (**HTTP 500**, und die Laufzeit sackte auf **19,5 ms** gegen 141–146 ms im regulären bekannten Zweig — der BF-137-Angleich wurde also tatsächlich übersprungen), gegengeprüft (302 auf `/de/login`, Laufzeit wieder über der Mindestdauer). ⚠ **Der Zugangsverlust bleibt und ist von hier aus nicht heilbar** — an eine Adresse, die kein Mailserver annimmt, ist kein Link zustellbar. Deshalb geht eine `warning` ins Protokoll (in `prod` an Sentry), mit der **Kennung** statt der Adresse (`send_default_pii: false`); helfen kann danach nur ein Mensch. ⚠ Kein Token mehr im Fehlerfall: Ein Schreibzugriff für einen Link, den niemand bekommt, ist keiner. `Bf138ResetRfcAdresseTest` prüft Statuscode, Gleichheit zum unbekannten Zweig, die Mindestdauer und dass keine Mail entsteht. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-136 | 01 | **Die Kontolöschung prüft das Passwort ohne Rate Limit.** Gemessen: **20 Fehlversuche in Folge, alle 302**, kein 429, keine Meldung. Gegenprobe an derselben Geheimnisprüfung beim Passwortwechsel im selben Durchlauf: „Zu viele Versuche. Bitte warte 15 Minuten" ab dem sechsten (BF-20). ⚠ Dasselbe Bedrohungsmodell — Passwort-Raten aus einer **gekaperten Sitzung** —, aber schwerere Folge: Die Löschung ist **unumkehrbar** und kennt laut Decision Log keine Karenzzeit. ⚠ `LimiterCoverageTest` prüft, ob **deklarierte** Limiter verdrahtet sind, nicht ob einer **fehlt** — ein nie angelegter Limiter ist für ihn unsichtbar. Betrifft laufenden Code: Das Feature ist live. Vom `code-reviewer` gefunden, durch Angriff belegt | **hoch** | `src/Controller/ProfileController.php:210-252`; kein `account_delete` in `config/packages/framework.yaml` | **behoben 2026-09-12** — Limiter `account_delete` (3 je 15 Minuten, am Konto), Verbrauch VOR der Passwortprüfung und HINTER der CSRF-Prüfung. Am Server gegengeprüft: 3 Versuche durch, ab dem 4. „Zu viele Löschversuche" — **auch mit dem richtigen Passwort** (Konto 1 → 1). Fremdes Kontingent lässt sich nicht leerlaufen: nach 6 Versuchen mit ungültigem CSRF-Token gehen weiterhin 3 echte durch. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-137 | 01 | **Die Anti-Enumeration des Passwort-Resets ist über die Laufzeit umgehbar.** Die Antwort ist identisch (beide 302 → `/de/login`), die Laufzeit **trennscharf**: bekannt 31–36 ms, unbekannt 23–24 ms, Median-Differenz 12 ms, keine Überlappung über je sieben Messungen. Eine einzige Messung genügt zur Unterscheidung. Ursache: Bei bekannter Adresse entstehen Token, `flush()` und Mail-Dispatch, bei unbekannter nichts — die Registrierung löst dasselbe Problem mit einem Hash in **beiden** Zweigen (BF-09). ⚠ Der Limiter (5/Stunde) verhindert den Massenabgleich, nicht die gezielte Einzelfrage | mittel | `src/Controller/PasswordResetController.php` (Anfrage-Zweig) | **behoben 2026-09-12** — beide Zweige halten eine Mindestdauer (`MINDESTDAUER_SEKUNDEN = 0.12`). Am Server gegengeprüft: bekannt 141–146 ms, unbekannt 144–145 ms — die Bereiche **überlappen** (vorher trennscharf 31–36 gegen 23–24). ⚠ Der Angleich wird übersprungen, wenn im bekannten Zweig eine Ausnahme fliegt — siehe BF-138. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-131 | B01 | **AK-12 ist in Produktion unerreichbar — und ein grüner Prüflauf verdeckt es.** `SendEmailMessage: async` lässt `send()` nur in die Warteschlange stellen; die `TransportExceptionInterface` und damit die Warnung an den Nutzer treten nie ein. `testMailerFailureShowsWarningAndStillRedirects` ist trotzdem grün, weil das Test-Env `sync` fährt. ⚠ Dieselbe Ursache wie BF-124 (B14/AK-04), hier aber mit einem **grünen Test über dem Kriterium** — schlechter als kein Test | mittel | `src/Controller/RegistrationController.php:143-149`; Ursache `config/packages/messenger.yaml:60` vs. `:77` | **behoben 2026-09-12** — `testMailerFailureShowsWarningAndStillRedirects` **erzeugte gar keine Störung**: Der werfende Mailer wurde über `$client->getContainer()->set(MailerInterface::class, …)` eingesetzt, und das erreicht den Dienst-Locator des Controllers nicht, weil der Mailer ein Methodenargument ist. Gemessen: Der Ersatz warf, und es gingen trotzdem zwei Mails hinaus — geprüft wurde am Ende nur, dass eine ungestörte Registrierung weiterleitet. Der Lauf stört jetzt echt (`MAILER_DSN=smtp://127.0.0.1:1`) und sichert zusätzlich zu, dass die Meldung wirklich `warning` ist; Rückbau der Warnung färbt ihn rot. ⚠ Sein Docblock sagt nun ausdrücklich, dass er den **Test-Env**-Fall belegt und nicht das Verhalten auf Produktion — dort ist der Zweig strukturell unerreichbar (siehe BF-124). AK-12 ist in `spec.md` entsprechend eingeschränkt. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-132 | B01 | **Sechs von neun Fehlbestand-Punkten sind erledigt, drei DSGVO-Pflichten darunter.** FB-01, FB-02 (Limiter, BF-02), FB-04 (Kontolöschung), FB-05 (Passwort-Reset, auf Produktion HTTP 200), FB-06 (Datenexport), FB-07 (`user.email_unique`). Dazu AK-14 und AK-15 überholt. ⚠ Zusätzlich: `design.md` Entscheidung #6 behauptet `sync://` auf Produktion (seit 2026-09-02 falsch), und `spec.md:86` nennt eine **einzige** `isVerified()`-Prüfung — es sind drei. **Eine Datenschutz-Auskunft anhand dieser Spec wäre falsch, zuungunsten des Betreibers.** Dritte Ausprägung nach BF-126 (B14) und BF-130 (B15), und die schwerste | mittel | `features/B01-registrierung-email-bestaetigung/spec.md` (AK-14, AK-15, FB-01, FB-02, FB-04 bis FB-07, Zeile 41, 86), `design.md` (#6, :159, :161) | **behoben 2026-09-12** — `spec.md` und `design.md` von B01 abgeglichen. Durchgestrichen und mit Nachweis versehen: FB-01, FB-02, FB-04, FB-05, FB-06, FB-07 und zusätzlich FB-09 (trusted_hosts, BF-134 vom selben Tag). Berichtigt: AK-14 (generische Antwort seit BF-09), AK-15 (Routen-Kollision behoben), EC-02 und EC-05, die Zahl der `isVerified()`-Prüfungen (**drei** statt einer, dazu eine vierte mit anderer Wirkung im Brevo-Abgleich — die Zeilenangabe war ebenfalls falsch: 39, nicht 29), die Folge von FB-03 (milder als beschrieben) sowie `design.md`-Entscheidung 6 und die AK-12-Zeile der Abdeckungstabelle, die beide `sync://` auf Produktion behaupteten. AK-12 selbst trägt jetzt die Einschränkung aus BF-124/BF-131. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-133 | projektweit | **Feature `01` ist gebaut, gemerged und live — steht im Index aber auf `roadmap`.** Routen `app_password_reset_request`, `app_password_reset`, `app_profile_export`, `app_profile_delete` vorhanden; `PasswordResetController` auf `master`; `https://endlech.lu/de/passwort-vergessen` → **HTTP 200**. Der Ordner enthält **nur `spec.md`** — kein `design.md`, keine `tasks.md`, kein Prüfbericht. ⚠ Das ist die Ursache von BF-132: Weil der Bau nie gebucht wurde, blieb der Fehlbestand von B01 stehen. **Kein Prüflauf kann das finden** — nichts hält den Index gegen die vorhandenen Routen | mittel | `features/index.md` (Zeile zu Feature 01), `features/01-betroffenenrechte/` | **behoben 2026-09-12** — der Index stand bereits durch die QA vom 2026-09-12 auf `approved`; nachgetragen sind jetzt die fehlenden Artefakte: `design.md` (Struktur, Zugriffsregeln, Missbrauchsschutz, zehn Entscheidungen, Abdeckung aller 24 Kriterien) und die Zeile in der Umfangstabelle des Index, die schlicht fehlte. ⚠ **Kein `tasks.md`, und das ausdrücklich:** Ein nachträglich erfundener Aufgabenplan für fertigen Code belegt nichts und täuscht eine Kette vor, die hier nie gelaufen ist — der Vermerk steht im Kopf der `design.md`. ⚠ Jede Aussage der Datei ist am Code gegengeprüft; zwei Entwürfe waren falsch und wurden berichtigt (die App-Vormerkung **ist** Teil des Exports, und `scheduleRemoval()` steht vor dem `remove()`). **Die Ursache bleibt unbewacht:** Nichts hält den Index gegen die vorhandenen Routen. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-134 | B01 / B14 / B15 | **Der Bestätigungslink folgt dem `Host`-Header.** Lokal ausgenutzt: mit gefälschtem `Host` **und passendem `Referer`/`Origin`** → HTTP 302, Konto angelegt, Link in der Mail lautet `http://boeser-server.example/de/verify/…`. ⚠ Mit bloß gefälschtem `Host` antwortet die Anwendung 422 (stateless-CSRF) — wer nur das probiert, hält die Lücke für geschlossen. ⚠ **Auf Produktion greift der Angriff nicht**: Coolifys Proxy weist einen fremden Host mit **503** ab (gemessen). Diese Verteidigung steht nirgends geschrieben und fällt weg, sobald jemand ohne diesen Proxy betreibt. `trusted_hosts` ist bis heute nicht gesetzt (FB-09) | mittel | `config/packages/framework.yaml` (kein `trusted_hosts`); Linkbau in `RegistrationController` | **behoben 2026-09-12** — `framework.trusted_hosts` trägt jetzt `^(www\.)?endlech\.lu$`, `^localhost$`, `^127\.0\.0\.1$` (feste Liste, keine Umgebungsvariable — BF-29). Angriff gegengeprüft: mit gefälschtem `Host`, `Referer` und `Origin` **HTTP 400 statt 302**, kein Konto, keine Mail in der Warteschlange. ⚠ **Die lokalen Einträge sind Pflicht**: Der `HEALTHCHECK` des Images ruft `http://127.0.0.1/health`; ohne den Eintrag antwortet die Anwendung dort 400, der Container gilt als krank und Coolify rollt zurück (BF-116-Muster). Vor der Änderung gemessen, welchen Host Symfony live sieht: `endlech.lu` bzw. `www.endlech.lu` (aus dem Location-Header), **kein** Container-Name. `Bf134TrustedHostsTest` prüft vier erlaubte Hosts über HTTP und drei fremde gegen die geladene Konfiguration. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-135 | B01 | **Bei Versandfehlern verrät die Antwort doch, ob die Adresse vergeben ist.** Neuanlage-Zweig setzt `warning`/`flash.register_email_failed`, der Bestandszweig verschluckt dieselbe Exception und fällt in `success`. ⚠ Der Kommentar dort lautet „Die Antwort bleibt in jedem Fall dieselbe" — sie bleibt nur *innerhalb* dieses Zweigs dieselbe. ⚠ Heute nicht auslösbar, weil `async` keine `TransportExceptionInterface` wirft (siehe BF-131); **wird scharf, sobald jemand auf `sync://` zurückstellt**. Im Code belegt, am laufenden System nicht nachgestellt. Vom `code-reviewer` gefunden — die Verhaltensprüfung hatte nur den Erfolgsweg gemessen | mittel | `src/Controller/RegistrationController.php:143-149` vs. `:174-178` | **behoben 2026-09-12** — `sendeKontoExistiertHinweis()` gibt jetzt `bool` zurück, und der Bestandszweig setzt dieselbe Meldung wie der Neuanlage-Zweig (`warning`/`flash.register_email_failed` bei Störung, sonst `success`). Reproduziert mit unerreichbarem SMTP-Port: neue Adresse `warning`, bestehende `success` — der Unterschied war die Antwort auf „gibt es dieses Konto?". ⚠ **Der naheliegende Prüfweg trägt nicht:** `$client->getContainer()->set(MailerInterface::class, …)` erreicht den Dienst-Locator des Controllers nicht (der Mailer ist ein Methodenargument) — der Ersatz warf, und es gingen trotzdem zwei Mails hinaus. Gestört wird deshalb über `MAILER_DSN=smtp://127.0.0.1:1`; der Wert muss im `tearDown()` **zurückgestellt** werden, nicht gelöscht, sonst ist `%env(MAILER_DSN)%` undefiniert und der nächste Aufruf endet mit 500 (gemessen). ⚠ Damit steht fest, dass `testMailerFailureShowsWarningAndStillRedirects` nie eine Störung erzeugt hat — siehe BF-131. `Bf135RegisterVersandfehlerTest` prüft Gleichheit, Erhalt der Warnung und den ungestörten Fall. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-129 | B15 | **Der Organisationsweg meldet über den Partner-Schlüssel.** `OrganisationController:93` ruft `flash.partner_rate_limited`. ⚠ Der Schaden ist kleiner, als AK-14 annimmt: Der hinterlegte **Text** ist in allen vier Sprachen neutral („Sie haben in kurzer Zeit mehrere Anmeldungen abgeschickt") — ein Besucher liest nichts Falsches, irreführend ist allein der Schlüsselname. Die zweite Hälfte von AK-14 („geteilter Limiter") ist durch BF-38 überholt, am Server gemessen | niedrig | `src/Controller/OrganisationController.php:93` | **behoben 2026-09-12** — eigener Schlüssel `flash.organisation_rate_limited` in allen vier Katalogen; der Wortlaut nennt jetzt die **Verbindung** statt „Sie“, weil hinter einer Gemeindeverwaltung viele Menschen eine Adresse teilen und der Deckel dort jemanden trifft, der selbst nichts abgeschickt hat (BF-38). ⚠ Der Grund, einen so kleinen Befund zu beheben, liegt in der Zukunft: Der Schlüsselname ist die Stelle, an der jemand den Text später ändert — wer ihn für die Partnerseite anpasst, hätte stillschweigend die Organisationsseite mitgeändert. `testBf129EigeneMeldungBeiErschoepftemKontingent` prüft 429 **und** dass die Partner-Meldung dort nicht erscheint; Rückbau färbt rot. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-130 | B15 | **Spec und Entwurf beschreiben an sechs Stellen einen überholten Stand.** AK-14, AK-15, AK-16 sowie **FB-01, FB-03, FB-06** und `design.md:75`. FB-01 („kein Widerrufsweg") und FB-03 („kein Tokenablauf") sind dort als **DSGVO-Lücken** erfasst (Art. 7 Abs. 3, Art. 5 Abs. 1 lit. e), obwohl seit dem 2026-08-29 behoben und ausgeliefert — wer die Datenschutzlage anhand der Spec beurteilt, kommt zu einem falschen Ergebnis. ⚠ Drei der sechs Stellen kamen vom `code-reviewer`; der prüfende Agent hatte nur die AK-Tabelle angesehen, nicht den Fehlbestand und nicht `design.md`. ⚠ FB-02 („keine Aufräumroutine") ist **nicht** betroffen und weiterhin offen | mittel | `features/B15-organisations-wartelisten/spec.md` (AK-14 bis AK-16, FB-01, FB-03, FB-06), `design.md:75` | **behoben 2026-09-12** — alle sechs Stellen berichtigt: AK-14 (zweite Hälfte überholt, der Schlüsselname bleibt als BF-129), AK-15 (Tokenfrist und Widerrufsweg seit BF-36/37 vorhanden — die Zeile führte zwei **DSGVO-Lücken** weiter, die zwei Wochen zuvor geschlossen worden waren), AK-16 (zwei fehlende Felder), FB-01, FB-03 und FB-06, dazu FB-04 (BF-134) und die Limiter-Tabelle in `design.md:75`, die noch `limiter.partner_waitlist` — „geteilt mit B14“ nannte. ⚠ **FB-02 ist nicht betroffen und bleibt offen**; das steht jetzt ausdrücklich dort, damit der nächste Abgleich es nicht versehentlich mitstreicht. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-127 | B14 | **Zwei von drei Angriffsadressen im BF-119-Prüflauf sichern nichts ab.** Der Docblock von `rfcWidrigeAdressen()` erklärt alle drei zu Adressen, „die der HTML5-Default durchließ" — gegen `PATTERN_HTML5` gemessen trifft das nur auf `../../etc/passwd@example.lu` zu; `a"b(c)d@…` und `jemand@@…` weist schon der Default ab. Unabhängig belegt durch die Rückbau-Gegenprobe: mit entferntem STRICT wurde **1 von 3** Datensätzen rot. Der Test bleibt fachlich richtig, schützt gegen ein Zurückdrehen aber nur zu einem Drittel. ⚠ Vom `code-reviewer` gefunden, nicht vom prüfenden Agenten — der hatte die 1-von-3-Messung gesehen und als Beobachtung abgetan | niedrig | `tests/Functional/Controller/Bf119EmailValidierungTest.php:36-41` | **behoben 2026-09-12** — die beiden wirkungslosen Datensätze sind ersetzt durch drei Formen, die der HTML5-Default nachweislich durchlässt (doppelter, führender und abschliessender Punkt im Local-Part). Rückbau-Gegenprobe nach der Änderung: **4 von 4** Datensätzen rot, vorher 1 von 3. ⚠ **Die eigentliche Reparatur ist `Bf127AngriffsadressenTest`** — er prüft die Liste selbst: jeder Eintrag muss vom Validator im HTML5-Modus angenommen und von `Mime\Address` abgelehnt werden, und STRICT muss ihn abweisen. Gemessen wird gegen die Bibliothek, nicht gegen eine abgeschriebene Kopie des Musters; ohne diesen Lauf kann der nächste gut gemeinte Datensatz wieder wirkungslos einschlafen. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-128 | projektweit | **`CLAUDE.md` behauptet weiterhin, BF-119 sei für B14, B15 und B01 offen** („nutzen weiterhin den Default und haben denselben Fehler"). Genau diese drei Formulare sind repariert. Der Satz ist eine ⚠-Warnung — die Textsorte, auf die sich in diesem Projekt jeder verlässt — und führt ab dem Merge in die Irre. ⚠ Der Reviewer nannte zusätzlich `features/befunde.md`; dort war die Zeile zu dem Zeitpunkt bereits fortgeschrieben (geprüft und verworfen) | niedrig | `CLAUDE.md:751-752` | **behoben 2026-09-11** — Abschnitt sagt jetzt, dass alle vier Wege strikt prüfen, und benennt zusätzlich die zweite Hälfte der Reparatur (Adressprüfung vor dem `flush()`) samt der Warnung, die Reihenfolge nicht umzudrehen. ⚠ Von `sdd-deploy` angefasst, auf ausdrückliche Weisung — die Liste zu führen ist Aufgabe von `sdd-qa`. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-124 | B14 / projektweit | **Bei gescheiterter Zustellung bekommt der Nutzer keine Warnung mehr — stille Regression seit dem 2026-09-02.** Der erste Durchlauf belegte AK-04 ausdrücklich als bestanden („Bei gestopptem Transport ... bekam der Nutzer eine verständliche Meldung"); damals lief der Mailer synchron. Seit `SendEmailMessage: async` wirft `MailerInterface::send()` keine `TransportExceptionInterface` mehr, `register()` gibt immer `true` zurück, und `if (!$sent)` ist unerreichbar. Nachgestellt mit gestopptem Mailpit: **HTTP 302 ohne Warnung**, Eintrag entsteht (5 → 6), Nachricht in `messenger_messages`. ⚠ Kein Datenverlust — die Ursache steht in `CLAUDE.md` („zwölf Blöcke toter Code"), die **Folge für AK-04 und AK-16 hat niemand gezogen**. Betrifft alle acht Dateien mit diesem Muster | mittel | `src/Controller/PartnerController.php:104`; Ursache `config/packages/messenger.yaml:60` | **behoben 2026-09-12** — soweit es hier behebbar ist: Der Zweig **bleibt** (er trägt im Test-Env und in jeder Aufstellung ohne async-Routing, und BF-135 hängt daran), aber die Zusage ist berichtigt. `register()` dokumentiert jetzt „angenommen ist nicht zugestellt" und dass die Methode auf Produktion immer `true` liefert; alle drei Aufrufer (Partner, Organisation, App-Warteliste) tragen den Hinweis am `if (!$sent)`. ⚠ **Ein falscher Kommentar mitkorrigiert:** Der Service behauptete „Auf Production fällt es heute nicht auf, weil dort synchron versendet wird (`sync://`); es kippt, sobald ein Messenger-Worker dazukommt" — der Worker läuft seit dem 2026-09-02, `->locale()` ist damit nicht Vorsorge, sondern das Einzige, was die Sprache der Mail trägt. `MailVersandRoutingTest` hält die Entscheidung fest (geparst, nicht durchsucht — BF-125). ⚠ **Was nicht heilbar ist:** Den Nutzer kann bei asynchronem Versand niemand mehr warnen; zum Zeitpunkt der Antwort ist die Zustellung nicht passiert. Für den Betreiber tritt `app:messenger:watch` an die Stelle — als Einschränkung bei AK-04 (B14) und AK-12 (B01) vermerkt. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-125 | B14 | **Ein Prüflauf ist grün, weil er einen Kommentar liest.** `testAk23BeideWartelistenTeilenSichDenLimiter` durchsucht den Quelltext von `OrganisationController` als String nach `limiter.partner_waitlist`. Der Ausdruck steht dort seit BF-38 nur noch im **Kommentar** von Zeile 37; die `#[Autowire]`-Zeile nennt `limiter.organisation_waitlist`. Der Test bestätigt damit das Gegenteil des tatsächlichen Verhaltens und bliebe auch bei einem Rückbau grün — in beide Richtungen blind. Sein eigener Kommentar sagt, er dürfe nach BF-38 fallen | mittel | `tests/Functional/Controller/PartnerControllerTest.php:250-261` | **behoben 2026-09-12** — der Lauf heisst jetzt `testAk23WartelistenFuehrenGetrennteKontingente` und prüft **Verhalten**: Das Partner-Kontingent wird in einem Zug erschöpft (Limit 10000 im Test-Env), danach antwortet der Partnerweg mit 429 und der Organisationsweg weiterhin mit 302. Rückbau-Gegenprobe: Zeigt `OrganisationController` wieder auf `limiter.partner_waitlist`, wird der Lauf rot (429 statt 302) — die Vorfassung wäre in **beide** Richtungen grün geblieben. ⚠ Der Zähler liegt im Cache-Pool und übersteht den DAMA-Rollback, deshalb `reset()` **vor** den Zusicherungen (BF-136, dieselbe Falle). **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-126 | B14 | **Die Spec beschreibt an fünf Stellen einen überholten Stand.** AK-21 („keine Ablauffrist"), AK-22 („kein Widerrufsweg") und AK-23 („geteiltes Kontingent") sind durch BF-36/37/38 erledigt und seit `v2026.08.29` live; AK-17 zählt die erfassten Daten abschließend auf, es sind aber drei Spalten mehr (`marketing_consent_at`, `self_confirmed_at`, `restaurant_id`); AK-04 siehe BF-124. ⚠ Wer die Spec heute liest, hält drei behobene DSGVO-relevante Mängel für offen. Genau die Falle, vor der `CLAUDE.md` bei `B`-Features warnt | mittel | `features/B14-partner-warteliste/spec.md` (AK-04, AK-17, AK-21 bis AK-23) | **behoben 2026-09-12** — alle fünf Stellen der B14-`spec.md` berichtigt: AK-21 (Frist sieben Tage, HTTP 410), AK-22 (Abmeldelink in jeder Mail) und AK-23 (eigenes Kontingent, am 2026-09-12 über Verhalten belegt) sind durchgestrichen mit Nachweis; AK-17 nennt die drei fehlenden Felder (`marketingConsentAt`, `selfConfirmedAt`, `restaurant`) — eine Auskunft nach der alten Liste wäre lückenhaft gewesen; AK-04 trägt die Einschränkung aus BF-124. Mitgezogen: FB-01, FB-03 und FB-04 (BF-134). ⚠ **FB-02 ausdrücklich nachgeprüft und weiterhin offen** — `findPendingOlderThan()` wird im Produktivcode nach wie vor nirgends gerufen, der Aufräumlauf aus Feature 08 greift nur auf die App-Warteliste. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-80 | 02 / projektweit | Bei 768 px scrollen **alle** Seiten um 51 px waagerecht — Startseite, `/about`, `/open`, `/restaurants`, `/criteria`, `/legal` und die Vergleichsseiten. Mit ausgeblendetem `<header>` sind es 0 px; bei genau 768 px greift die Desktop-Navigation, der Platz reicht nicht, `flex-wrap: nowrap` verhindert den Umbruch. **2026-08-30 bei der QA von `05` nachgemessen und verschärft:** abgemeldet +36 px bei 768 px und ab 850 px weg — **angemeldet aber +81 px, und das Band reicht bis unter 1000 px** (850 px → +40, 900 px → +15). Bei 768 px messen Logo (123) + Navigation (416) + Kontobereich (250 bzw. 295 angemeldet) zusammen 789 bzw. 833 px, während der Inhaltsbereich nach `px-4` nur 736 px fasst. Vollständige Messreihe: `docs/app-shell.md`, Bekannte Lücke 7 | mittel | `templates/base.html.twig` — `div.flex items-center gap-4` | **behoben 2026-09-12** — vier Änderungen an der Kopfzeile, alle gemessen: Der Logo-Schriftzug erscheint erst ab `lg` (das Bild bleibt), der Nutzername ebenso (der Avatar bleibt), die Abstände sind enger (`space-x-3` im md-Bereich, `lg:space-x-6` statt `8`, `gap-3` im Kontobereich, `px-3` im Kopf), und das Logo trägt `shrink-0`. **Gemessen mit einem headless Chrome gegen den laufenden Dev-Server** (Seite mit `<base>` lokal gespeichert, Messskript angehängt, `document.title` über `--dump-dom` ausgelesen): angemeldet **81 → 0 px** bei 768 px, 40 → 0 bei 850, 15 → 0 bei 900; abgemeldet 36 → 0. Gegengeprüft auf `/restaurants`, `/open`, `/vergleich`, `/presse`, `/about` (alle 0) und in **allen vier Sprachen**. ⚠ **Französisch war schlimmer als der Befund sagte: 149 px**, und es fiel erst auf, nachdem `shrink-0` das Logo daran hinderte, den Überlauf zu verstecken (es war auf 30 px zusammengedrückt). ⚠ **Bei `lg` war die Reserve auf 1 px zusammengelaufen** (Bedarf 991, verfügbar 992) — deshalb sind auch die Desktop-Abstände enger. ⚠ **Was bleibt: zwei Navigationspunkte brechen bei 768 px zweizeilig um** (48 px statt 24). Das ist Bestand und war vorher schlimmer (Französisch dreizeilig, 72 px); mit `whitespace-nowrap` an allen vier Punkten wäre der Umbruch weg und das Scrollen zurück (rund 52 px). Als bewusster Kompromiss im Template vermerkt. ⚠ **Der Breakpoint wurde NICHT verschoben** — die naheliegende Lösung (Kopfnavigation erst ab `lg`) hätte `/about` im Band 768–1023 px unerreichbar gemacht: Die Seite steht ausschliesslich in der Kopfnavigation, nicht in der Fussleiste, und die Bottom-Nav ist dort verborgen. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-82 | 03 | Ein Anbietername von 57 Zeichen **ohne Leerzeichen** sprengt bei 320 px die Kartendarstellung (`scrollX=104`). Bis 30 Zeichen sauber; die realen Wortmarken sind 8–11 Zeichen | niedrig | `templates/comparison/_cards.html.twig` — `<dt>` ohne `overflow-wrap` | **behoben 2026-09-12** — `body { overflow-wrap: break-word }` in `app.css`, dazu `min-w-0` an den beiden Karten des Eignungsblocks. Gemessen mit einem 48 Zeichen langen Namen ohne Leerzeichen: **182 → 74 → 0 px** Überlauf. ⚠ **Zwei Fehlschläge auf dem Weg, beide lehrreich:** (1) `break-words` an der Stelle, die den Namen rendert, traf daneben — gemessen überstanden Überschrift und **Fliesstext** des Eignungsblocks, denn der Name steckt auch dort; `overflow-wrap` wird vererbt, deshalb steht die Regel einmal am `body`. (2) Die Regel allein liess 74 px übrig, weil `overflow-wrap: break-word` die **min-content-Breite nicht ändert** und ein Grid-Item `min-width: auto` hat — erst `min-w-0` gab die Box frei. ⚠ Nicht `anywhere` oder `break-all`: die zerlegen auch normale Wörter (BF-77). ⚠ Die Regel wirkt projektweit, und das ist Absicht — Restaurantnamen, Ideen-Titel und Organisationsnamen sind **nutzergeneriert**, dort ist ein überlanges Wort keine Vorsorge. Sieben Seiten gegengeprüft, alle 0 px. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-90 | 04 | Nach einem `contactDeleted` bleibt bei zwei Quellen eine Zeile auf `synced` stehen, obwohl der Kontakt bei Brevo gelöscht ist: `record()` verweigert wegen der Sperre, `scheduleRemoval()` gibt `null` zurück. **Kein Datenabfluss** — bei Brevo steht nichts mehr —, aber ein lokaler Zustand, der nicht mehr stimmt, und eine Zeile, die niemand aufräumt | niedrig | `src/Marketing/MarketingContactRegistry.php` — `scheduleRemoval()` | **behoben 2026-09-12, mit einer Richtigstellung** — der lokale Zustand war **nicht** falsch: `MarketingSyncState` sagt laut eigenem Docblock, was Brevo noch **schuldet**, nicht ob dort ein Kontakt steht; nach `contactDeleted` schuldet Brevo nichts, und die Zeile bleibt zu Recht auf `synced`. Sie **muss** auch stehen bleiben: Sie ist die Sperre, ohne die der nächste Lauf die Adresse in fünf Minuten neu einträgt (AK-12) — „eine Zeile, die niemand aufräumt“ wäre hier ein Aufräumen, das schadet. ⚠ **Eine Brevo-Fremdkennung, die veralten könnte, gibt es nicht**: Adressiert wird über `ext_id`, also über die eigene Zeilen-Kennung. Echt war dagegen die **Anzeige**: In Liste und Detailansicht stand grün „Übertragen“ samt Datum, ohne dass die wirksame Sperre erkennbar war — wer das liest, hält den Kontakt für aktiv bespielt. Beide Ansichten tragen jetzt ein „Gesperrt“-Abzeichen (`marketing.admin.blocked`, vier Sprachen), geprüft von `testBf90SperreWirdInDerListeAusgewiesen`; Rückbau färbt rot. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-88 | 04 | Der AV-Vertrag mit Brevo ist in `docs/datenschutz.md` **nicht festgehalten**, sondern als „noch zu prüfen" markiert; das Prüfdatum fehlt (AK-33). Hängt an **OF-01**, der nie festgelegten Datenschutzstufe des Projekts. Keine Softwarefrage — aber die Freigabe-Sperre für den ersten echten Lauf | mittel | `docs/datenschutz.md` | **erledigt, festgestellt am 2026-09-12** — der Befund war veraltet. `docs/datenschutz.md` nennt den AV-Vertrag (**Annex 2 der Brevo General Terms and Conditions**, gilt automatisch mit Annahme der AGB), die gelesene Fassung (**Stand 15.05.2024**), den Ablageort der Kopie (`qa/brevo-dpa/BREVO-Annex-2-DPA-150524.pdf`, vorhanden) und das **Prüfdatum 2026-09-05** — also genau das, was AK-33 verlangt. ⚠ Nichts davon stand in dieser Liste; dieselbe Klasse wie BF-110 und BF-88 selbst. **OF-01** (die nie festgelegte Datenschutzstufe des Projekts) bleibt davon unberührt offen. |
| ~~BF-93~~ | 05 | **behoben 2026-08-30 (QA³)** · Betreiberangaben fehlten auf `/presse` **und** `/legal`: Das Faktenblatt zeigt dreimal „Wird derzeit ergänzt", das Impressum unverändert „Endlech.lu / Luxemburg". AK-11 durchgefallen, AK-15 nicht prüfbar. **Kein Codeanteil** — die Mechanik (ein Parameter, zwei Seiten) steht und ist geprüft, die Werte fehlen (VB-03) | hoch | `config/services.yaml:40–42` | **behoben** — Michael Ferreira als Betreiber und Verantwortlicher; `OperatorDetailsTest` läuft statt zu überspringen. ⚠ Die **Anschrift bleibt bewusst leer** (OF-04), damit kann AK-11 nicht bestehen |
| ~~BF-94~~ | 05 | **behoben 2026-08-30 (QA³)** · Kein Bildmaterial und kein Paket: vier von fünf Vorschauen laufen in HTTP 404, kein Downloadlink. AK-16 und AK-20 durchgefallen, AK-17/18/19/22 nicht prüfbar. **Kein Codeanteil** — `app:press:package` verweigert bewusst ein halbes Paket (Exit 1), drei Prüfläufe überspringen mit benannter Begründung (VB-01) | hoch | `public/presse/` · `src/Press/PressRegistry.php::assets()` | **behoben** — vier Marken aus `logo.png` nachgezeichnet (0,244 % Abweichung), `make press-kit` gelaufen, Paket mit sechs Dateien |
| ~~BF-106~~ | 06 | **behoben 2026-08-30** · Ein Titel aus einem langen Wort ohne Leerzeichen sprengt das Board** — 80 × „W" (innerhalb der erlaubten 120 Zeichen) ergibt bei 320 px **1089 px Überhang**, bei 390 px 1019 px. Isoliert belegt: Karte im DOM entfernt → 0 px. ⚠ **Das ist BF-82 zum zweiten Mal** — dort aus einer festen Anbieterliste (*niedrig*), **hier aus Nutzereingabe**. Zweites Auftreten = fehlende projektweite Regel für Nutzertext (`overflow-wrap: anywhere`) | mittel | `templates/partials/_board_idea_card.html.twig` — Titel und Beschreibung ohne Umbruchregel | **behoben** — `wrap-anywhere` an acht Stellen über vier Templates; nachgemessen 0 px Überhang bei 320 und 390 px. ⚠ **BF-82 bleibt offen**, die Design-System-Regel ist nicht gezogen |
| ~~BF-107~~ | 06 | **behoben 2026-08-30** · Kurze Titel ergeben ein 36 × 44 px großes Ziel** — die Reparatur von BF-104 setzt mit `min-h-[44px]` nur die Höhe, nicht die Breite. AK-47 verlangt 44 × 44. ⚠ `BoardTargetSizeTest` fängt es nicht: Er liest die Klasse aus dem Markup und sieht keine gerenderte Breite | mittel | `templates/partials/_board_idea_card.html.twig` | **behoben in zwei Anläufen** — `w-full` allein reichte nicht (die Überschrift schrumpft im Flex-Container), `flex-1` deckte den nächsten Fall auf (37 px neben dem Statusabzeichen bei 320 px). Endstand `basis-full min-w-0 sm:basis-auto sm:flex-1`; null zu kleine Ziele bei 320 und 390 px |
| ~~BF-104~~ | 06 | **behoben 2026-08-30** · Die Titel-Verweise auf dem Board sind 18 px hoch** — gemessen bei 390 px im Browser (`a 192×18`, `a 202×18`, `a 157×18`, `a 82×18`). Sie sind der **einzige** Weg von der Liste in die Einzelansicht. AK-47 verlangt 44 × 44, WCAG 2.2 AA (2.5.8) mindestens 24 × 24 — beides verfehlt. ⚠ Auf einer Plattform für Menschen mit motorischen Einschränkungen wiegt das schwerer als anderswo, und Feature `02` sagt WCAG 2.2 AA über den vollen Bestand zu | hoch | `templates/partials/_board_idea_card.html.twig` — der `<a>` im `<h3>` | **behoben** — `min-h-[44px] flex items-center`; nachgemessen 192×44 (390 px) und 140×48 (320 px), null zu kleine Ziele. ⚠ Der Stretched Link wurde versucht und verworfen: korrekt gesetzt, im Stapel aber wirkungslos |
| ~~BF-105~~ | 06 | **behoben 2026-08-30** · Der committete `public/build` ist veralteter Stand** — `line-clamp-3` fehlt im CSS, obwohl nur Feature 06 die Klasse benutzt. Ein `npm run build` erzeugt sofort einen Diff in vier Dateien; `verify-assets` in `cd.yml` **blockiert damit den Deploy**. ⚠ Ursache ist ein Denkfehler, den der Aufgabenplan vorgab („`npm run build` entfällt, kommt ohne Änderung unter `assets/` aus"): **Tailwind v4 scannt `templates/`** (`@source "../../templates"`), nicht nur `assets/` | hoch | `public/build/` · Ursache: `features/06-community-feedback-board/tasks.md:19` und der entsprechende Satz in `CLAUDE.md` | **behoben** — gebaut, Determinismus belegt (identische Prüfsummen beim dritten Lauf); `BuiltAssetsTest` meldet den Fall künftig im normalen Prüflauf. ⚠ **Der Satz in `CLAUDE.md` ist noch nicht erweitert** — projektweite Änderung, gehört nicht in einen Fehlerauftrag |
| ~~BF-101~~ | 06 | **behoben 2026-08-30** · Die Deckel-Meldung nennt **keine Wartezeit**, obwohl AK-59 sie ausdrücklich verlangt. Der Deckel selbst greift exakt (5 durch, 6. → 429, kein Datensatz). ⚠ `ActionLimiter::retryAfter()` ist vorhanden und wird im Projekt an **vier** anderen Stellen genau dafür benutzt — `AccessibilityController.php:64` rechnet sogar in Minuten um. Hier nicht | mittel | `translations/messages.*.yaml` → `flash.board_rate_limited`; `src/Controller/BoardController.php:109` | **behoben** — `wartezeitInMinuten()` nach dem Muster aus `AccessibilityController`; live belegt („in 45 Minuten erneut“) |
| ~~BF-102~~ | 06 | **behoben 2026-08-30** · Beim Zustimmen erscheint „Zu viele **Einreichungen**" — derselbe Übersetzungsschlüssel wird für beide Wege benutzt und benennt den falschen Vorgang | niedrig | `src/Controller/BoardController.php:199` | **behoben** — eigener Schlüssel `flash.board_vote_rate_limited` in vier Sprachen; live belegt („Zu viele Zustimmungen … in 49 Minuten“) |
| BF-103 | 06 | **`endlech.userjot.com` nimmt weiterhin Einreichungen entgegen** (AK-81). Der Fußzeilenverweis zeigt bereits aufs eigene Board — damit ist das externe zwar unverlinkt, über Suchmaschinen und Lesezeichen aber weiter auffindbar. Beiträge landen dort, wo niemand sie liest: genau die Sackgasse, gegen die Feature 06 gebaut wurde. **Keine Softwarefrage** — ein Handgriff im Anbieter-Konto. ⚠ **Reihenfolge: NACH dem Deploy** (Betreiberentscheidung 2026-08-30). Davor wäre userjot der einzige Rückmeldeweg; ihn zu schließen, bevor `/community/ideen` live ist, erzeugte ein Fenster ganz ohne Weg | mittel | außerhalb des Repositorys | **erledigt 2026-09-12 durch den Betreiber, nachgeprüft am selben Tag.** `endlech.userjot.com` antwortet mit **HTTP 500** (`{„status“:500,„unhandled“:true,„message“:„HTTPError“}`), dreimal reproduziert; dieselbe Antwort liefern `/ideas`, `/ideas/new`, `/board`, `/p/new` und `/api/v1/posts`. **Es gibt dort keinen Einreichweg mehr.** ⚠ **Der 500er ist der Nachweis und nicht bloss ein Ausfall — belegt über zwei Gegenproben:** Eine frei erfundene Subdomain (`gibtesnicht-endlech-12345.userjot.com`) antwortet **identisch**, 500 ist bei diesem Anbieter also die Antwort auf „dieses Board existiert nicht“; und der Dienst selbst (`userjot.com`) antwortet mit **200**, läuft also. ⚠ Die Reihenfolge wurde eingehalten: Das eigene Board ist seit `v2026.08.31` live, `https://endlech.lu/de/community/ideen` mit **HTTP 200** gemessen — es gab also keinen Augenblick ohne Rückmeldeweg. ⚠ **Was bleibt:** Wer den alten Link im Lesezeichen hat, sieht eine nackte JSON-Fehlerseite statt eines Hinweises aufs neue Board. Das liesse sich nur beim Anbieter einrichten, und dafür hätte das Board bestehen bleiben müssen; jetzt ist es weg. Suchmaschinen verlieren die Adresse mit der Zeit von selbst. |
| BF-95 | 05 | Eine fehlende Vorschaudatei erzeugt ein Bruchbild statt eines Ersatzes — anders als beim Paket, wo der Kontaktweg an die Stelle des Knopfes tritt. Der Entwurf sieht einen Fehlerzustand nur für `PressPackage` vor. Braucht **erst ein Kriterium** (OF-09), dann Code | mittel | `templates/press/_material.html.twig:18–22` | **behoben 2026-09-12 — und damit OF-09 beantwortet: ein Prüflauf statt eines Fehlerzustands.** `PressRegistryTest::testJedeMaterialdateiLiegtUnterPublic` verlangt für jede Datei der Materialliste, dass sie unter `public/` liegt und nicht leer ist; eine fehlende Datei färbt den Lauf rot, statt dem Leser ein Bruchbild zu zeigen. Gegengeprüft durch Wegnehmen einer SVG-Datei. ⚠ **Gegen den Laufzeit-Fehlerzustand (Kachel ausblenden) sprechen drei Dinge:** Er kostete bei jedem Seitenaufruf vier Dateisystemzugriffe für einen Fall, der nur durch einen Fehler im Repository entsteht; eine ausgeblendete Kachel verbirgt den Mangel vor dem Besucher **und** vor dem Betreiber; und die Dateien sind committet, ihre Existenz ist zur Bauzeit entscheidbar. Muster wie `RouteDirectoryCollisionTest` (BF-100): die Ursache prüfen, nicht das Verhalten abfangen. ⚠ Die vier SVG-Dateien liegen seit dem 2026-08-30 vor — der Befund beschrieb einen Zustand, den VB-01 inzwischen behoben hat. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| ~~BF-96~~ | 05 | **behoben 2026-08-30 (QA³)** · Der Fotocredit nannte keinen Urheber („Bildnachweis wird ergänzt"). Die Nutzungsbedingung steht, die Urheberangabe fehlt — ein Presse-Kit, das ein Foto ohne Urheberangabe ausgibt, verursacht das Problem beim Abdruckenden (AK-24, OF-05) | mittel | `translations/press.*.yaml` → `person.photo_credit` | **behoben** — Fotocredit nennt den Urheber, in vier Sprachen |
| ~~BF-100~~ | 05 | **behoben 2026-08-30** · Der sprachfreie Kurzlink `/presse` läuft auf Produktion in eine endlose Weiterleitungsschleife.** Das neue Verzeichnis `public/presse/` kollidiert mit der Route: Apaches `mod_dir` schickt `/presse` per **301** auf `/presse/` (weil ein Verzeichnis existiert), Symfonys Trailing-Slash-Regel schickt `/presse/` per 301 zurück auf `/presse`. Gemessen am 2026-08-30 direkt nach dem Deploy: 50 Weiterleitungen, dann Abbruch. `/vergleich` und `/open` sind unberührt — dort gibt es kein gleichnamiges Verzeichnis. **Lokal nicht sichtbar**, weil der Symfony-Entwicklungsserver kein `mod_dir` hat. AK-05 ist damit auf Produktion gebrochen — und das ist genau die Adresse, die „in Mails an Redaktionen und auf Visitenkarten steht". ⚠ Der erste Sprung ist ein **301**: Browser merken ihn sich dauerhaft. Wer die Adresse einmal geöffnet hat, läuft auch nach einer Reparatur weiter im Kreis, solange `/presse/` seinerseits auf `/presse` zurückleitet | **kritisch** | `public/presse/` gegen `app_press_redirect` in `config/routes.yaml` | **behoben** — Verzeichnis heißt `presse-kit`, und **eine** Route matcht `/presse` und `/presse/` exakt (`{trailing_slash}`), sodass beide Formen direkt mit 302 auf die Sprachfassung gehen. `RouteDirectoryCollisionTest` hält die Ursache fest. **Ausgeliefert mit **v2026.08.30.1**** |
| BF-99 | 05 | Der Schriftzug der beiden Wort-Bildmarken liegt als `<text>` vor, nicht als Pfad. Ohne die Schrift auf dem Zielsystem ersetzt der Betrachter sie — bei einer Wortmarke ist die Schrift die Marke. Die Bildmarke selbst ist sauber vektorisiert. Kein Codeanteil: einmal in Illustrator oder Affinity outlinen, dann `make press-kit` erneut | mittel | `public/presse/endlech-wortbildmarke.svg`, `…-invers.svg` | **behoben 2026-09-12** — die Marke hat jetzt eine Schrift: **Inter 4.1** (SIL Open Font License 1.1, Einbetten und Outlinen ausdrücklich erlaubt), selbst gehostet, auf Latin zugeschnitten (vier Schnitte, je ~17 kB, zusammen 80 kB). Der Schriftzug beider Wort-Bildmarken liegt als **Pfad** vor. ⚠ **Nicht „in Illustrator outlinen“:** Das hätte die Schrift des jeweiligen Rechners eingefroren — auf diesem Mac SF Pro, das für Markenzwecke nicht lizenziert ist. Die Pfade sind erzeugt: geshaped mit **HarfBuzz** (also mit dem Kerning der Schrift), mit **fontTools** in SVG-Koordinaten gerechnet, `letter-spacing: -13` und `dominant-baseline: central` mitgerechnet. ⚠ **Gegengeprüft über die BBox:** Text-Fassung (x=1555.0, w=2452.7) gegen Pfad-Fassung (x=1588.0, w=2401.8) — die Differenz ist **genau** die linke und rechte Seitenlage der Randglyphen (33 und 18 Einheiten), und die Oberkante liegt 363,8 Einheiten über der Grundlinie, also exakt auf Inters Versalhöhe. Beide Fassungen zusätzlich als PNG gerendert und angesehen. ⚠ **Die Kopfzeile hing daran:** Inter ist breiter, und die Messung zählt die Bildlaufleiste mit (753 statt 768 px) — Französisch lief wieder über (17 px als Gast). Nachgezogen: Logo-Schriftzug erst ab `xl`, „Über uns" erst ab `lg` und dafür **in der Fussleiste ergänzt** (dort fehlte der Punkt ganz), Abstände gestaffelt. Gemessen: 0 px in vier Sprachen bei 768/1024/1280/1440 px. ⚠ `CACHE_VERSION` in `public/sw.js` auf `v2` erhöht (Pflicht, sobald eine Datei unter `public/presse-kit/` ersetzt wird), Paket neu gebaut, `docs/design-system.md` berichtigt — dort stand „kein einziger benannter Design-Token“. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| ~~BF-97~~ | 05 | **behoben 2026-08-30** · **Mit vorhandenem Materialpaket antwortet `/presse` in allen vier Sprachen mit HTTP 500.** `_material.html.twig:44` ruft `package.publicPath`; auf `PressPackage` ist der Pfad eine **Klassenkonstante**, und Twig löst `object.attr` nie über eine Konstante auf. Der Fehler liegt im **Regelfall** des Features und blieb verborgen, weil die Umgebung kein Paket hat — der einzige Lauf, der den Abschnitt anfasst, prüfte nur den Ersatzzweig | **kritisch** | `templates/press/_material.html.twig:44` gegen `src/Press/PressPackage.php:22` | **behoben, gegengeprüft 2026-08-30 (QA²)** — `PressPackage::publicPath()`; mit angelegtem Paket 200 in allen vier Sprachen, `PressPackageTest` läuft erstmals durch statt zu überspringen. **Ausgeliefert mit **v2026.08.30**** |
| ~~BF-98~~ | 05 | **behoben 2026-08-30** · Zusammengesetzte Übersetzungsschlüssel (`'material.allowed_' ~ i`, die sechs `facts.*_value`) fallen durch **beide** Netze: `CatalogueCompletenessTest` erfasst nur Literale, `PressCatalogueTest` nur die von `PressRegistry` genannten. Entfernt man einen aus allen vier Katalogen, bleibt die Suite grün und die Seite zeigt den rohen Schlüssel. Heute ist nichts kaputt — die Absicherung fehlt. Derselbe blinde Fleck wie BF-56 | mittel | `tests/Unit/Translation/PressCatalogueTest.php:129–154` | **behoben, gegengeprüft 2026-08-30 (QA²)** — vierzehn Schlüssel als `ZUSAMMENGESETZTE_SCHLUESSEL`; beide Mutationsproben unabhängig wiederholt, beide werden rot, nach dem Wiederherstellen grün. **Ausgeliefert mit **v2026.08.30**** |
| ~~BF-108~~ | 07 | **Bei 768 px ist der Titel jeder Community-Karte eine senkrechte Buchstabenkolonne** — 12 px breit, 648 px hoch; beim 120-Zeichen-Titel 20 × 2352 px. Die kuratierten Einträge derselben Spalte sind dort 155 px breit. Messreihe: 320 px → 64, 375 px → 119, **768 px → 12–20**, 1280 px → 176. AK-15 fällt durch: Der Titel steht im Markup und ist nicht lesbar. ⚠ **Das ist BF-107 zum zweiten Mal** — dieselbe Bauart (Titel neben `shrink-0`-Abzeichen im Flex-Container), dort in zwei Anläufen gelöst, hier beim Neubau der Karte nicht mitgenommen. ⚠ Übersehen, weil nur 320 px und 1280 px gemessen wurden — an beiden Enden unauffällig | hoch | `templates/roadmap/_item.html.twig` — Titel ohne `min-w-0`/`flex-1` | **behoben 2026-08-30** — `flex-wrap` am Container, `basis-full min-w-0 lg:basis-auto lg:flex-1` am Titel. ⚠ Umbruchpunkt `lg:` statt `sm:` wie in `06`: Dort füllt die Karte die Seitenbreite, hier eine von drei Spalten — ab `md:` misst sie 229 px, mit `sm:` wäre der gemessene Fall stehen geblieben. Nachgemessen 320/375/768/1280 px → 214/269/155/326 px, gleichauf mit den kuratierten Einträgen; Titelhöhe von 2352 auf 168 px. **Neu: `RoadmapCardLayoutTest` prüft das Muster über beide Kartenvorlagen**, zwei Gegenproben. **Ausgeliefert mit **v2026.08.31**** |
| ~~BF-112~~ | 07 | **Der Prüflauf zum Changelog wird bei jedem Release rot.** `RoadmapControllerTest::testChangelogZeigtNeunReleasesUndDieSammelzeile` prüft mit `assertCount(10, …)` gegen eine **fest verdrahtete Zahl**. Beim Release `v2026.08.30.3` schlug er fehl (`actual size 11 matches expected size 10`) — obwohl alles richtig war. ⚠ **Nicht kosmetisch:** Der fünfte Punkt der Release-Checkliste erzeugt damit systematisch einen roten Lauf; ein Prüflauf, der bei jedem *korrekten* Vorgang anschlägt, wird nach dem dritten Mal ignoriert oder abgeschaltet — und dann fehlt genau die Absicherung, für die er gebaut wurde. **Gefunden im Deploy-Preflight**, nicht in der QA: Erst der Release selbst stellt den Zustand her | mittel | `tests/Functional/Controller/RoadmapControllerTest.php:210` | **behoben 2026-08-30** — die erwartete Zahl wird aus `ChangelogRegistry` **abgeleitet statt genannt**. Der Lauf prüft damit mehr als vorher: nicht eine Momentaufnahme, sondern die **Kopplung** zwischen Registry und Seite. Zwei Gegenproben: ein weiteres Release → **grün** (der BF-112-Fall ist weg), ein vom Template verschluckter Eintrag → **rot** mit Nennung der erwarteten Zahl (Prüfkraft erhalten). **Ausgeliefert mit **v2026.08.31**** |
| ~~BF-113~~ | 07 | **`assertCount(8, …)` für die zurückgestellten Punkte** — dieselbe Bauart wie BF-112, eine Datei weiter. Nachgestellt: Ein neunter Eintrag in `RoadmapRegistry::shelved()` macht `testZurueckgestelltesStehtAusserhalbDerSpalten` rot („actual size 9 matches expected size 8"), obwohl alles richtig ist. ⚠ **Der Auslöser ist der Regelbetrieb**, keine Hypothese: OF-03 sieht eine Roadmap-Durchsicht bei jedem Release vor, OF-04 die Rückstufung nach zwölf Monaten — Einträge wandern also planmäßig | mittel | `tests/Functional/Controller/RoadmapControllerTest.php:190` | **behoben 2026-08-30** — Zahl aus `RoadmapRegistry::shelved()` abgeleitet. Reproduktion (neunter Punkt) → grün; Gegenprobe (Template verschluckt einen) → rot mit Nennung der erwarteten Zahl. **Ausgeliefert mit **v2026.08.31**** |
| ~~BF-114~~ | 07 | **Die Jahres-Prüfungen nehmen an, dass es nur ein Jahr gibt** — und das endet spätestens mit dem ersten Release in 2027. Nachgestellt mit einem Eintrag vom 15.01.2027: **zwei** Läufe werden rot. (1) `testDasLaufendeJahrIstOffenDasFruehereZugeklappt` erwartet feste `<details>`-Zahlen (0 bzw. 1) — „Failed asserting that 1 is identical to 0". (2) `testDieGruppierungNachJahrenIstVollstaendig` verlangt die Sammelzeile als älteste Zeile **jedes** Jahres — 2027 hat keine: „Failed asserting that an instance of ReleaseNote is an instance of ChangelogSummary". ⚠ **Tritt sicher ein**, nicht nur möglicherweise | mittel | `tests/Functional/Controller/RoadmapFreshnessTest.php:127` und `:110` | **behoben 2026-08-30** — beide Stellen: Die Zahl der zugeklappten Jahre wird abgeleitet (jedes Jahr außer dem laufenden), und die Sammelzeile wird nur noch in **ihrem eigenen** Jahr verlangt. Auch der Datenlieferant führt keine festen Jahreszahlen mehr, sondern das jüngste Jahr mit Einträgen und eines ohne. Reproduktion (Release 2027) → grün; Gegenprobe (laufendes Jahr fälschlich zugeklappt) → rot. **Ausgeliefert mit **v2026.08.31**** |
| ~~BF-115~~ | 07 | **Ein zugeklapptes Jahr trägt keine Überschrift** — das laufende Jahr bekommt `<h2 id="year-…">`, ein früheres nur ein `<summary>`. Für einen Screenreader ist das keine Gliederung: Die Einträge darin tragen `h3` und hängen an keiner übergeordneten Ebene; die Kette springt von `h1` auf `h3` (WCAG 1.3.1, AK-38). ⚠ **Heute unsichtbar, tritt sicher ein:** Solange die Registry nur ein Jahr führt, gibt es kein `<details>`. Mit dem ersten Release in einem neuen Jahr rutscht das bisherige hinein und verliert seine Überschrift. Gefunden durch eine **Vollbetriebs-Probe** (zwei Jahre in der Registry), nicht durch Lesen. ⚠ **Anders als BF-109 gehört dieser Befund dem Feature**, nicht der App-Hülle | mittel | `templates/roadmap/changelog.html.twig` — der `<details>`-Zweig der Jahresschleife | **behoben 2026-08-30** — `<summary><h2 class="inline">…</h2></summary>`; HTML erlaubt dort Heading-Content, `inline` hält sie neben dem Aufklapp-Dreieck (gemessen: 21 px hoch in einem 60 px hohen `<summary>`, kein Umbruch). Mit hergestellter Reproduktion am HTML nachgemessen: Kette `h1 → h2 → h3 → h2 → h3` — **keine Sprünge**. Gegenprobe: Überschrift entfernt → beide Läufe rot. `RoadmapYearHeadingTest` jetzt grün. **Ausgeliefert mit **v2026.08.31**** |
| ~~BF-116~~ | 06 | **Der Deploy vom 2026-08-31 ist gescheitert und hat die Seite offline genommen.** `cache:clear` bricht auf Produktion ab: *„The target-entity `App\Entity\self` cannot be found in `App\Entity\BoardIdea#duplicateOf`."* Ursache: `#[ORM\ManyToOne]` **ohne `targetEntity`** bei einem Property vom Typ `?self` (`src/Entity/BoardIdea.php:83–85`). Doctrine leitet das Ziel aus dem Typ ab; auf **PHP 8.4** (Produktion) wird `self` nicht zur Klasse aufgelöst, sondern als Klassenname genommen. ⚠ **Lokal nicht reproduzierbar** — hier läuft PHP 8.5.2, dort greift die Auflösung. Damit ein zweiter Fall der Sorte „lokal ≠ Produktion" neben MySQL 8 / MariaDB 10.5 und `mod_dir` (BF-100): **jetzt auch die PHP-Version.** ⚠ Der Abbruch kam im `composer install`-Post-Script, also **vor** `doctrine:migrations:migrate` — die Datenbank blieb unverändert, der Rollback war gefahrlos. **Vorschlag:** `#[ORM\ManyToOne(targetEntity: self::class)]` — `self::class` wird zur Übersetzungszeit aufgelöst | **kritisch** | `src/Entity/BoardIdea.php:83` | **behoben 2026-08-31** — `#[ORM\ManyToOne(targetEntity: self::class)]`; `self::class` wird zur Übersetzungszeit aufgelöst und ist von der Sprachversion unabhängig. ⚠ **Lokal nicht reproduzierbar** (PHP 8.5.2 statt 8.4): Der Nachweis ist deshalb ein **statischer** Prüflauf, `MappingSelfTargetTest` — keine Assoziation mit dem Property-Typ `self` darf ohne explizites `targetEntity` stehen. Projektweit geprüft: **genau eine** Stelle war betroffen. Zwei Gegenproben: `targetEntity` entfernt → rot; **neue Entity mit demselben Muster** → rot. **Am 2026-08-31 unter PHP 8.4 reproduziert und belegt** (`docker run --rm -v "$PWD":/app -w /app php:8.4-cli`): Ohne die Reparatur löst Doctrine `targetEntity` zu **`App\Entity\self`** auf — Klasse existiert nicht, exakt die Produktionsmeldung. Mit der Reparatur zu `App\Entity\BoardIdea`. Ursache bestätigt: `ReflectionProperty::getType()->getName()` liefert unter **8.4.25 `self`**, unter **8.5.2 `App\Entity\BoardIdea`**. **Ausgeliefert mit **v2026.08.31**** |
| BF-109 | 07 / projektweit | Die Fußzeile überschreibt ihre vier Spalten mit `<h4>`; da die letzte Inhaltsüberschrift eine `h2` ist, springt die Überschriftenkette **jeder Seite** von h2 auf h4 (WCAG 1.3.1). axe meldet `heading-order` auf allen acht Aufrufen von `/roadmap` und `/changelog`. **Vorbestehend und projektweit**: nachgemessen auf `/presse` (`…,2,4,4,4`), `/open`, `/about`, `/vergleich`, `/community/ideen`. Innerhalb von `<main>` ist die Kette auf beiden neuen Seiten lückenlos. Blockiert AK-34 und AK-38 von Feature 07, gehört aber zur App-Hülle | mittel | `templates/base.html.twig` — vier `<h4>` in der Fußzeile | **behoben 2026-09-12** — die drei Spaltentitel der Fussleiste stehen jetzt als `h2` statt `h4`. `h2` und nicht `h3`, weil die Fussleiste ein eigener Bereich (`contentinfo`) ist und ihre Spalten gleichrangig zu den Hauptabschnitten stehen; die Schriftgrösse hängt an den Klassen, nicht am Tag (Tailwinds Preflight setzt Überschriften auf `font-size: inherit`). Reproduziert auf **sieben von neun** geprüften Seiten (`/`, `/about`, `/presse`, `/open`, `/roadmap`, `/changelog`, `/community/ideen` — je Kette `…,2,4,4,4`); `/restaurants` und `/legal` waren unauffällig, weil ihr Inhalt auf h3 endet. ⚠ **Die Reparatur ist der Prüflauf:** `HeadingOrderTest` sammelt die Überschriften von neun Seiten in Dokumentreihenfolge und verlangt eine lückenlose Kette **plus genau eine `h1`** — ohne den zweiten Teil liesse sich die Lücke schliessen, indem jemand aus der Fussleiste eine zweite Hauptüberschrift macht. Damit blockiert der Befund AK-34 und AK-38 von Feature 07 nicht mehr. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-110 | 07 / projektweit | Der `hreflang`-Block übernimmt die Abfragezeichenfolge in die vier Alternativ-Verweise: `/de/roadmap?stage=secret` erzeugt `<link rel="alternate" href="/lb/roadmap?stage=secret">`. Damit erscheint eine Eingabe des Aufrufers in der Antwort, obwohl AK-44 das ausschließt. **Kein Sicherheitsproblem** — escaped, `<script>` bleibt wirkungslos (0 rohe Treffer gemessen). **Projektweit**: `/presse`, `/open`, `/about`, `/restaurants` spiegeln ebenso | niedrig | `templates/base.html.twig:43` — `url(_current_route, _current_params\|merge(…))` | **richtiggestellt und bewacht 2026-09-12 — die Fundstelle war falsch.** Gemessen: Die `hreflang`-Verweise sind **frei** von der Abfragezeichenfolge; der Block baut seine Adressen aus `_route_params`, und darin stehen nur Routenvariablen. Wer den Befund wörtlich nahm, reparierte eine Stelle, die nichts tut. Die drei Treffer im Dokument stammen vom **Sprachumschalter** (`templates/partials/_language_switcher.html.twig`), und dort ist die Übernahme seit BF-68 Absicht: **sieben Controller** werten Abfrageparameter aus (Restaurantliste, Ideen-Board, Organisationsseiten, vier Verwaltungsseiten), ein Sprachwechsel, der die Filter wegwirft, wäre ein Rückschritt. Eine Positivliste erlaubter Parameter oder Routen wäre die nächste Pflegefalle. ⚠ **Festgeschrieben ist deshalb die Eigenschaft, auf die es ankommt** (`QueryParameterReflexionTest`): Ein Fremdparameter darf in der **Query-Position** eines eigenen Verweises stehen, niemals in **Pfad-Position** und niemals ungeschützt — genau das trennt den harmlosen Fall von BF-68, wo `?_locale=//fremd.example/de` den Browser auf einen fremden Host schickte. Gemessen: `?foo=//fremd.example` landet nur in der Query, `<script>` erscheint 0× roh, und die Filter überleben den Sprachwechsel. ⚠ **AK-44 ist damit nicht wörtlich erfüllt** — „keine Eingabe erscheint in der Antwort“ ist als Zusage zu weit gefasst: Jede gefilterte Liste reflektiert ihre Filter, auch in den Formularfeldern. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-111 | 06 | **Eine wartende Idee ohne Verfasser ist für jeden öffentlich lesbar.** `if (!$idea->isPublished() && $this->getUser() !== $idea->getSubmittedBy())` — bei einem Gast ist `getUser()` `null`, bei einer verfasserlosen Idee `getSubmittedBy()` ebenfalls; `null !== null` ist **false**, die Sperre greift nicht. Nachgestellt: HTTP **200**, Titel in `<title>` und im Seitenkopf, Beschreibung vollständig. ⚠ **Heute nicht erreichbar** — `AccountDeleter` löscht wartende Ideen vor dem Konto, der Fremdschlüssel (`SET NULL`) kommt nicht zum Zug. Die Prüfung ist richtig aus dem falschen Grund: Ein zweiter Anlageweg (Bestandsimport, Verwaltung, Datenbankeingriff) öffnet sie, und `testFremdeWartendeIdeeErgibt404` bemerkt es nicht — er prüft nur den Fall **mit** Verfasser. Gefunden bei der QA von `07` | mittel | `src/Controller/BoardController.php:169` | **behoben 2026-09-12** — die Bedingung deckt den Fall „gar kein Verfasser" eigens ab: Eine unveröffentlichte Idee sieht nur ihr Verfasser, und gibt es keinen, sieht sie niemand. Reproduziert (HTTP 200, Titel im `<title>`, Beschreibung sichtbar) und gegengeprüft (HTTP 404, Beschreibung 0×). ⚠ Nicht zu `$user === $verfasser` vereinfachen — bei zwei `null` wäre das wieder wahr. `testBf111WartendeIdeeOhneVerfasserErgibt404` prüft Gast und fremdes Konto und dass der Titel auch in der 404-Antwort fehlt; mit der alten Bedingung rot. ⚠ Zeile 235 (`withdraw`) trägt dasselbe Muster, ist aber durch `#[IsGranted('ROLE_USER')]` gedeckt — dort ist `getUser()` nie `null`. **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |
| BF-119 | B14 / B15 / B01 | **Eine Adresse, die Symfonys `Email`-Constraint passiert, aber nicht RFC 2822 entspricht, erzeugt HTTP 500 — und hinterlässt trotzdem eine Zeile.** `new Email(...)` läuft im HTML5-Default und lässt `../../etc/passwd@example.lu` durch; `Mime\Address` verlangt RFC 2822 und wirft. Weil `register()` **vor** dem Versand speichert (Reihenfolge Token → flush → Mail), steht der Datensatz bereits: gemessen 0 → 1 Zeile. ⚠ **Gegenprobe am Partner-Formular (B14): dieselbe Ausnahme.** `PartnerWaitlistType`, `OrganisationWaitlistType` und `RegistrationType` nutzen alle den HTML5-Default — der Befund gehört als eigener Auftrag in die Bestandsfeatures | hoch | `PartnerWaitlistType.php:51`, `OrganisationWaitlistType.php:89`, `RegistrationType.php:36`; Auslösung in `src/Waitlist/WaitlistConfirmationService.php:93` | **behoben 2026-09-11** — alle drei verbliebenen Formulare (`PartnerWaitlistType`, `OrganisationWaitlistType`, `RegistrationType`) prüfen jetzt mit `Email::VALIDATION_MODE_STRICT`; zusätzlich wandert die Adresskonstruktion in `WaitlistConfirmationService::register()` und `RegistrationController` **vor** den `flush()`, damit ein ungültiger Wert keine Zeile hinterlässt. ⚠ Die Reihenfolge Token → flush → Mail bleibt unangetastet — getrennt wurden nur die beiden Fragen „ist das eine Adresse?" (davor) und „kam die Mail an?" (dahinter). Gegenprobe am laufenden Server: **422 statt 500**, Bestand **5 → 5**. Rückbau beider Reparaturen macht die neuen Prüfläufe rot (einzeln nachgestellt). Abgesichert durch `Bf119EmailValidierungTest` (27 Fälle) und `Bf119RegisterReihenfolgeTest` (Integration, ruft den Service direkt — über das Formular wäre die zweite Hälfte nicht mehr prüfbar). **Ausgeliefert mit `v2026.09.12.1`** (2026-09-12, auf Produktion nachgeprüft) |

Die drei Befunde des ersten `03`-Durchlaufs (BF-77/78/79), BF-81 sowie die vier der
`02`-QA und die 72 Rückerfassungs-Befunde sind behoben — siehe unten.

> **Alles davon liegt inzwischen auf `master`.** Die Spalte *Ausgeliefert* trug bis zum
> 2026-09-11 den Branchnamen und den Vermerk „noch nicht" — beides war überholt, ohne dass
> es jemandem auffiel: Die genannten Branches (`feature/02-barrierefreiheit-plattform`,
> `feature/03-vergleichsseiten`, `fix/befunde-abarbeiten`) sind längst gemerged und
> gelöscht, und `production` heißt seit dem 2026-09-02 `master`.
>
> ⚠ **Die Spalte trägt jetzt die Release-Version, nicht „behoben".** Ein Branchname
> beantwortet die Frage nicht, die hier steht: Ob ein Nutzer die Reparatur hat, hängt am
> Release, nicht am Merge. Ermittelt wurde sie je Befund über den Commit, der ihn einführt,
> und `git tag --contains` — nicht aus dem Behebungsdatum geschlossen. Gegenprobe: Für
> keinen der 87 Einträge liegt der zugeordnete Release **vor** dem dokumentierten
> Behebungsdatum.

## Behoben

### Feature 04 · Marketing-Kontakte in Brevo (QA 2026-08-29, behoben am selben Tag)

| ID | Befund | Grad | Behebung | Gegenprobe |
|---|---|---|---|---|
| BF-122 | 08 | **Die 30-Tage-Aufräumfrist lässt sich unbegrenzt verlängern.** `renewConfirmationWindow()` (die Reparatur zu BF-117) setzt `createdAt` zurück — und daran messen **zwei** Fristen: der Token-Ablauf (7 Tage, gewollt) und `deleteStaleUnconfirmed()` (30 Tage, **nicht** gewollt). Nachgestellt: `createdAt` springt von `2026-08-07` auf heute, der Lauf an Tag 31 löscht **0**. Ein POST alle 7–29 Tage genügt und kostet einen von zehn Token je Stunde; `handleDuplicate()` prüft keine Eigentümerschaft, funktioniert also mit **fremden** Adressen, die dabei je Runde eine Mail bekommen. ⚠ Auf dem Hauptweg ist AK-47 nachweislich erfüllt — umgangen wird es nur durch wiederholtes Zutun. **Vorschlag:** zweite Obergrenze am Löschkriterium über `consentAt`, das beim Erneuern unberührt bleibt und den Erstkontakt markiert | mittel | `src/Entity/AppWaitlistEntry.php` (`renewConfirmationWindow`) · `src/Repository/AppWaitlistEntryRepository.php` (`deleteStaleUnconfirmed`) | behoben 2026-09-05 · `deleteStaleUnconfirmed()` misst an **`consentAt`** statt `createdAt` — das Feld bleibt beim Erneuern unberührt und markiert den Erstkontakt. Damit bleibt der Bestätigungslink erneuerbar (AK-17), die Aufbewahrung aber gedeckelt (AK-47). Gegenprobe: Lauf an Tag 31 löscht **1** (war 0), auch nach fünf Erneuerungsrunden. ⚠ Die ursprüngliche Reproduktion alterte nur `created_at`; real hat ein 29 Tage alter Eintrag **beide** Zeitpunkte in der Vergangenheit — vor der Reparatur folgenlos, danach der Unterschied zwischen rot und grün. Ausgeliefert mit **v2026.09.05** |
| BF-117 | 08 | **Der bei einem abgelaufenen Vorgang neu ausgestellte Bestätigungslink ist sofort wieder abgelaufen.** `WaitlistConfirmationService::register()` erzeugt einen neuen Token, setzt `createdAt` aber nicht zurück — und `isExpired()` misst ausschließlich daran. Nachgestellt: nach erneutem Eintragen steht `createdAt` unverändert auf `-8 days`, der Aufruf des **neuen** Links liefert **HTTP 410**. Damit besteht genau die Sackgasse fort, die AK-17 verhindern soll. ⚠ Der Feature-Test prüfte, dass sich der Token **ändert**, nicht dass er **trägt** — dasselbe Muster wie BF-64 | hoch | `src/Waitlist/WaitlistConfirmationService.php:61-117` (kein `createdAt`-Reset), `:223-226` (`isExpired`) | behoben 2026-09-05 · `AppWaitlistEntry::renewConfirmationWindow()` setzt Frist **und** Token; `handleDuplicate()` ruft sie vor dem erneuten Versand. Gegenprobe: neuer Link → **HTTP 200** (war 410). Test `AppWaitlistRegressionTest::testBf117…` prüft die **Einlösung**, nicht die Änderung des Tokens. Ausgeliefert mit **v2026.09.05** |
| BF-118 | 08 | **Der Dublettenzweig verbraucht kein Rate-Limit-Kontingent — unbegrenzter Mailversand an fremde Adressen.** `handleDuplicate()` kehrt mit `return` zurück, bevor `$limiter->consume()` erreicht wird, verschickt im Ablauf-Zweig aber eine Mail. Gemessen: fünf Absendevorgänge auf eine abgelaufene fremde Vormerkung → fünf Mails, Restkontingent 9984 → 9984, **Differenz 0**. Zusammen mit BF-117 ist der Zustand dauerhaft, weil der Eintrag „abgelaufen" bleibt. Betrifft jede nie bestätigte Adresse zwischen Tag 8 und Tag 30. ⚠ `isAllowed()` ruft `consume(0)` und speichert nichts — das ist richtig und der Grund, warum der Verbrauch eine eigene Zeile ist | hoch | `src/Controller/AppWaitlistController.php:101-105` und `:154-156` | behoben 2026-09-05 · `handleDuplicate()` bekommt den `ActionLimiter` und verbraucht im Ablauf-Zweig vor dem Versand; der Zweig „bereits bestätigt" verbraucht weiterhin nichts. Gegenprobe: fünf Absendevorgänge → **1 Mail**, Kontingent-Differenz **1** (war 0 bei 5). Ausgeliefert mit **v2026.09.05** |
| BF-120 | 08 | **Die gewählte Plattform wird an Brevo übertragen, obwohl AK-54 und `design.md` sie ausdrücklich ausschließen** („Beide Felder bleiben leer"). `recordWaitlistEntry()` reicht `organisationName: $entry->getDisplayName()` für alle drei Wartelisten durch; bei B14/B15 ist das ein echter Organisationsname, bei `AppWaitlistEntry` das Plattform-Label. Gemessen in `marketing_contact`: `organisation_name = 'iOS'`, geht als Brevo-Attribut `ORGANISATION` hinaus. Kein Abfluss an Fremde und keine besondere Kategorie — aber ein Attribut in einer Werbeliste, das die Spezifikation abschließend ausschließt. ⚠ Der Entwurf hat genau diesen Fehler vorhergesagt und benannt | mittel | `src/Marketing/MarketingContactRegistry.php:102` · `src/Entity/AppWaitlistEntry.php:204-207` | behoben 2026-09-05 · `recordWaitlistEntry()` reicht für `AppWaitlistEntry` `organisationName: null` durch. Gegenprobe an der Zeile: leer, weder `ios` noch `android`. `getDisplayName()` bleibt — falsch war nicht die Methode, sondern sie ungeprüft als Marketing-Attribut weiterzureichen. Ausgeliefert mit **v2026.09.05** |
| BF-121 | 08 | **Ein echtes Wettrennen zweier gleichzeitiger Absendevorgänge wird nicht abgefangen.** Beide durchlaufen `findOneByEmail()` mit `null`, die zweite läuft beim `flush()` in die `UniqueConstraintViolationException`; es gibt kein `try/catch` und keinen Web-Exception-Subscriber (`ApiExceptionSubscriber` ist auf `^/api/v1` beschränkt). EC-06 verlangt ausdrücklich, dass „auch der Wettlauf abgefangen" ist. ⚠ **Im Code nachvollzogen, am laufenden System nicht reproduziert** — der Testclient ist einprozessig, das Fenster ein einzelner Datenbank-Roundtrip. Der häufigere Doppelklick-Fall (Zeile steht bereits) läuft nachweislich sauber: HTTP 302 | mittel | `src/Controller/AppWaitlistController.php:101-125` | behoben 2026-09-05 · `try/catch (UniqueConstraintViolationException)` um den Anlagepfad, im `catch` dieselbe `successResponse()` ohne erneuten Versand. Gegenprobe: **302 statt 500**, eine Zeile. Ausgeliefert mit **v2026.09.05** |
| BF-83 | Ein Verwaltungs-Statuswechsel befördert eine nie per Double-Opt-In bestätigte Adresse nach Brevo — der `confirmedAt`-Backfill stand vor dem Registry-Aufruf | **kritisch** | `applyStatus()` hält den Bestätigungsstand fest, **bevor** der Backfill läuft, und ruft die Registry nur dann | ⚠️ **Nur der erste Statuswechsel.** Der zweite liest ein `confirmedAt`, das der erste gesetzt hat — der Fehler tritt eine Runde später erneut auf. Fortgeführt als **BF-89** (QA², 2026-08-29) |
| BF-84 | Der Widerruf einer Quelle löscht den Kontakt einer anderen, aktiven Quelle mit derselben Adresse. Verstärker: Brevos `contactDeleted`-Echo der **eigenen** Löschung tilgte `marketing_consent_at` an allen Quellen | **kritisch** | `scheduleRemoval()` nimmt die auslösende Quelle entgegen und prüft über `aktiveQuellen()`, ob eine andere übrig bleibt — dann wird die Zeile auf jene umgeschrieben statt gelöscht. `blockByEmail()` steigt ohne vorhandene Zeile aus, und `contactDeleted` entwertet die Einwilligung an der Quelle nicht mehr | Über den echten Widerrufslink: Zeile bleibt (`pending/account`), Konto-Einwilligung steht, Wartelisten-Eintrag gelöscht. `MarketingBefundeTest::testBf84…`, `::testBf84b…` grün |
| BF-85 | `record()` war ohne zwischenzeitliches `flush()` nicht kollisionsfrei — `findOneByEmail()` sieht die vorgemerkte Zeile nicht | mittel | Die Registry führt eine Merkliste der in diesem Vorgang angelegten Zeilen; `finde()` sucht erst dort und verwirft Einträge, die der EntityManager nach `clear()` nicht mehr führt | `MarketingBefundeTest::testBf85…` grün. ⚠ Die echte Nebenläufigkeit (zwei parallele Requests) bleibt offen — siehe OF-09 |
| BF-86 | Eine fehlgeschlagene Übertragung wurde nie wieder aufgegriffen; `findOpenForSync()` fragte `FAILED` nicht ab, während der Enum-Kommentar das Gegenteil behauptete | hoch | `FAILED` steht in der Zustandsliste, der Rückzug bleibt allein beim Versuchszähler. Der Kommentar ist korrigiert und verweist auf die zweite Stelle | `MarketingSyncServiceTest::testAk19…` grün: nach 429 überträgt der zweite Lauf |
| BF-87 | Der Sync-Lauf committete bis zu 200 Zeilen ungeschützt in einer Transaktion | mittel | `flush()` in `try/catch`; ein Fehlschlag protokolliert Fehlerklasse und Zahl der betroffenen Zeilen (keine Adressen) und meldet sie im Ergebnis, statt den Cron-Lauf abzubrechen. Die Bündelung bleibt — wegen der Idempotenz unkritisch | 664 Tests grün |

| BF-89 | Der **zweite** Statuswechsel trug eine nie bestätigte Adresse ein — die BF-83-Reparatur griff nur eine Runde weit. Zweiter Weg: der Bestandsimport. Kehrseite: Nach dem Backfill lief der echte Bestätigungslink ins Leere | **kritisch** | Neues Feld **`selfConfirmedAt`** an beiden Wartelisten (Migration `Version20260829170000`), gesetzt **ausschließlich** von `confirm()`. Registry, `aktiveQuellen()` und die Import-Auswahl fragen `hasSelfConfirmed()`; `WaitlistConfirmationService::confirm()` prüft es für „bereits bestätigt". Der Vorabfilter in `applyStatus()` ist entfallen. ⚠ Der `confirmationToken` taugte **nicht** zur Unterscheidung — er bleibt in beiden Fällen stehen | Vier aufeinanderfolgende Statuswechsel → 0 Kontakte; echter Link danach → 1 Kontakt; AK-09 unverändert (`pending`); Import listet `NUR-BACKFILL` nicht, `ECHTER-DOI` schon; Doppelklick → 1 Kontakt. 674 Tests grün |

| BF-91 | Der Bestätigungsklick setzte einen fortgeschrittenen Vertriebsstatus zurück (`converted` → `confirmed`); der Rückfall wanderte über das Auftragsbuch bis in `FUNNEL_STATUS` bei Brevo (**AK-08**), und das Team bekam erneut eine „Neue Anmeldung"-Meldung | hoch | `confirm()` setzt den Status **nur noch aus `PENDING` heraus**, Zeitstempel weiterhin unbedingt. Beide Bestätigungs-Controller merken vor dem Aufruf, ob der Vorgang neu war (`$warNeu`), und rufen `notifyTeam()` nur dann | Eintrag auf `converted`: Status bleibt, 0 Team-Meldungen, `FUNNEL_STATUS=converted`, Selbstbestätigung festgehalten, Kontakt entsteht. Normalfall unberührt: `confirmed` + 1 Meldung. 677 Tests grün |
| BF-92 | `docs/data-model.md` führte keinen Bestandteil von Feature 04 | niedrig | Entity `MarketingContact` mit Feldreferenz, Indizes und den beiden ⚠-Begründungen; `marketingConsentAt` an allen drei Quellen, `selfConfirmedAt` an beiden Wartelisten, beide Enums, beide Migrationen | Alle neun Bestandteile in der Datei nachgewiesen. ⚠ Dabei fiel ein **vorbestehender Rückstand** auf: sechs Migrationen aus Feature 01/02 fehlen in der Historie — als Lücke vermerkt, nicht gefüllt |

⚠ **BF-88 und BF-90 bleiben offen** — beide stehen oben unter *Offen* und brauchen eine
Betreiberentscheidung. BF-88 (AV-Vertrag ohne Datum) hängt an OF-01; BF-90 (verwaiste
Zeile nach `contactDeleted`, niedrig) gehört mit OF-06 zusammen entschieden.


### Feature 03 · Vergleichsseiten (QA 2026-08-29, behoben am selben Tag)

| ID | Befund | Grad | Behebung | Gegenprobe |
|---|---|---|---|---|
| BF-77 | Die drei Vergleichsseiten scrollten bei 320 px waagerecht (`scrollX=212`); die Merkmalstabelle ist mit ihren erklärenden Halbsätzen 525 px breit | hoch | Kartendarstellung `_cards.html.twig` unter `md:`, Tabelle auf `hidden md:block`; gemeinsamer Inhalt in `_verdict_body.html.twig`. ⚠ Der naheliegende `overflow-wrap: anywhere` wurde ausprobiert und **verworfen** — er beseitigt das Scrollen und zerlegt dabei Wörter mitten im Wort | `scrollX=0` bei 320/375/1280 px auf allen drei Seiten und in allen Sprachen; zwei neue Prüfläufe, Rückbau schlägt fehl |
| BF-78 | `ComparisonGroup::transKey()` und `Verdict::transKey()` fielen durch jeden Prüflauf — der Schlüssel fehlte in allen vier Katalogen, 594 Tests blieben grün, der rohe Schlüssel stand auf der Seite | mittel | `schluesselAusDerRegistry()` läuft zusätzlich über `ComparisonGroup::cases()` und `Verdict::cases()` | Reproduktion greift nicht mehr: Lauf schlägt mit „1 Schlüssel fehlen in comparison.lb.yaml" fehl |
| BF-79 | Zwei `<nav>`-Landmarks je Vergleichsseite hießen beide „Weitere Vergleiche" | mittel | eigener Schlüssel `breadcrumb.label` in vier Sprachen | Landmarks heißen jetzt „Sie sind hier" / „Weitere Vergleiche"; neuer Prüflauf, Rückbau schlägt fehl |
| BF-81 | Der Fußnoten-Prüflauf filterte auf `table a[href^="#quelle-"]` und sah nur die Tabelle. Nachgestellt: Die Kartendarstellung verlor alle 18 Fußnotenlinks, **alle 606 Tests blieben grün** — auf schmalen Anzeigen hätten die Aussagen über den Wettbewerber unbelegt dagestanden | mittel | in der QA vom 2026-08-29 selbst geschlossen: neuer Prüflauf `testBeideDarstellungenBelegenIhreAussagen`, bestehender Test filtert nicht mehr auf `table` | Rückbau schlägt jetzt mit „Die Kartendarstellung belegt keine einzige Aussage" fehl |


| ID | Feature | Befund | Grad | Behoben am | Ausgeliefert |
|---|---|---|---|---|---|
| BF-123 | projektweit | **Die Anwendung liefert keine einzige Sicherheits-Kopfzeile.** Am 2026-09-11 bei der Deploy-Nachprüfung auf Produktion gemessen: Weder `Strict-Transport-Security` noch `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy` oder eine Inhaltsrichtlinie — die vollständige Antwort der Startseite führt neun Kopfzeilen, davon keine zur Sicherheit. Zusätzlich nennt `x-powered-by: PHP/8.4.25` die genaue Patch-Version des Interpreters. ⚠ **Vorbestehend, nicht durch ein Feature verursacht**: Das FrankenPHP-Image bringt sie nicht mit, eine eigene Caddy-Konfiguration gibt es nicht, und kein Prüflauf hat je danach gesehen. Ohne HSTS ist der erste Aufruf über `http://` angreifbar, obwohl die Seite ausschließlich verschlüsselt betrieben wird; ohne `frame-ancestors`/`X-Frame-Options` lässt sich die Verwaltung in einen fremden Rahmen setzen | mittel | 2026-09-11 | **noch nicht** — Release steht aus |
| BF-74 | 02 | Cookie-Banner fing beim `connect()` den Fokus; `focus()` nur noch in `reopen()` (nutzergetriggert) — Skip-Link ist wieder erstes Tab-Ziel (playwright PASS) | hoch | 2026-08-26 | **v2026.08.29** |
| BF-73 | 02 | Mailer fing nur `Mailer\TransportException`; catch um `Messenger\TransportException` erweitert (EC-04), Regressionstest ergänzt | niedrig | 2026-08-26 | **v2026.08.29** |
| BF-75 | 02 | Sortier-Leiste auf `/de/restaurants` mit `flex-wrap` versehen; kein 320px-Overflow mehr über alle öffentlichen Routen (AK-14) | mittel | 2026-08-26 | **v2026.08.29** |
| BF-76 | 02 | `focus:outline-none` durch echte `outline` ersetzt — `OpeningHourType`, `_passkey_manage`, 5 Profil-Felder; Fokus im Kontrastmodus sichtbar (AK-04/AK-40). Gefunden durch forced-colors + `AccessibilityInteractionTest` | mittel | 2026-08-26 | **v2026.08.29** |
| BF-04 | **01** | Feature `01` gebaut — Konto löschen, Daten exportieren, Passwort zurücksetzen, Einwilligung widerrufen | hoch | 2026-08-25 | **v2026.08.29** |
| BF-09 | B01 | Anti-Enumeration im Registrierformular; `UniqueEntity` in Gruppe `strict`, Hash in beiden Zweigen (528 vs. 522 ms) | mittel | 2026-08-25 | **v2026.08.29** |
| BF-10 | B14, B15, B23 | `->locale()` in `WaitlistConfirmationService` und `Api\V1\AuthController` | mittel | 2026-08-25 | **v2026.08.29** |
| BF-11 | B01, B14, B15 | `ActionLimiter` — Kontingent wird erst verbraucht, wenn die Handlung stattfindet | mittel | 2026-08-25 | **v2026.08.29** |
| BF-15 | B02 | `IS_AUTHENTICATED_REMEMBERED` für `/profile` | mittel | 2026-08-25 | **v2026.08.29** |
| BF-17 | B02 | `GuestLogoutSubscriber` — Weiterleitung statt 403, CSRF-Schutz unverändert | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-18 | B03 | Limiter `passkey_challenge` (30 je 15 min) über `RouteRateLimitSubscriber` | mittel | 2026-08-25 | **v2026.08.29** |
| BF-21 | B04 | Limiter `email_change` (3/h), am Konto gezählt | mittel | 2026-08-25 | **v2026.08.29** |
| BF-22 | B04 | Adresse wird auch im Fehlerfall zurückgesetzt | mittel | 2026-08-25 | **v2026.08.29** |
| BF-23 | B01, B04 | `!request` aus dem `fingers_crossed`-Puffer | mittel | 2026-08-25 | **v2026.08.29** |
| BF-29 | B23 | In `.env` erklärt, warum `trusted_hosts` nicht aus der Umgebung geht; `deploy.sh` warnt bei fehlendem `APP_API_BASE_URL` | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-30 | B23 | Limiter `suggestion_submit` (10/h), am Konto | mittel | 2026-08-25 | **v2026.08.29** |
| BF-31 | B23 | `id` → `submissionId` in der 202-Antwort | mittel | 2026-08-25 | **v2026.08.29** |
| BF-32 | B23 | `/me/submissions` zeigt Vorschläge mit `state` | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-33 | B19 | Referer ist nur noch Wegweiser; der Router baut das Ziel | mittel | 2026-08-25 | **v2026.08.29** |
| BF-34 | B19 | Sprachwechsel im Admin schreibt den Pfad um statt die Sitzung | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-35 | B19 | Limiter `admin_write` (120 je 5 min), GET zählt nicht | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-36 | B14 | Bestätigungslinks verfallen nach sieben Tagen, 410 statt 404 | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-37 | B14 | Abmeldelink in jeder Wartelisten-Mail; der Widerruf löscht | mittel | 2026-08-25 | **v2026.08.29** |
| BF-38 | B14 | Eigener Limiter `organisation_waitlist` | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-39 | B15 | „Verein" statt „Organisation", im Enum und in vier Katalogen | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-40 | B22 | Auswahlliste auf 50 begrenzt, serverseitige Suche | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-41 | B17 | `fieldNotes` in den Datensatz-Metadaten erklären `verified`, `assessed`, `accessibilityScore` | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-42 | B17 | Limiter `open_dataset` (60/h) mit `Retry-After` | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-43 | B17 | CSV-Werte mit führendem `= + - @` bekommen ein Apostroph (OWASP) | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-47 | B18 | Überschreiben ist ein eigener Knopf mit Rückfrage | mittel | 2026-08-25 | **v2026.08.29** |
| BF-48 | B18 | `src/Schedule.php` sagt selbst, dass es auf Production nicht feuert | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-49 | B11 | `assessedFeatures` hält fest, wonach jemand gesehen hat — `UNKNOWN` zählt nicht | mittel | 2026-08-25 | **v2026.08.29** |
| BF-50 | B11 | Limiter `suggestion_submit`, geteilt mit dem API-Weg | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-51 | B20 | `empty_data => ''` an sechs Pflichtfeldern — 422 statt 500 | mittel | 2026-08-25 | **v2026.08.29** |
| BF-52 | B20 | 25 je Seite plus Suche über Name und Stadt | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-53 | B20 | `RestaurantImageFileListener` auf `postRemove`, dazu `app:uploads:prune` | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-54 | B21 | Genehmigen und Ablehnen prüfen den Status — dreimal abgeschickt ergibt ein Restaurant | mittel | 2026-08-25 | **v2026.08.29** |
| BF-55 | B21 | Ablehnungsmail an den Einreicher, in der Sprache der Einreichung | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-56 | B21 | Türbreite und Tischabstand im Assistenten; wandern bei der Genehmigung mit | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-57 | B09 | MIME-Whitelist im `ImageUploadService`; Endung folgt dem echten Typ | mittel | 2026-08-25 | **v2026.08.29** |
| BF-58 | B09 | 4-MB-Grenze plus verständliche Meldung bei `post_max_size` | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-59 | B05 | LIKE-Platzhalter maskiert (`ESCAPE '!'`) | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-60 | B05 | Seite jenseits des Endes → 404 | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-61 | B07 | Folgetagsschleife läuft bis 7 statt 6 | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-62 | B08 | Längenprüfung am Endpunkt UND Slug-Kürzung im Repository (`ß` → 160 Zeichen) | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-63 | B08 | `DELETE /api/cuisines/{id}` für ungenutzte Typen, 409 mit `usedBy` | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-65 | B13 | Brevo, Verkéiersverbond, Betroffenenrechte, Speicherdauer, neuer Einleitungssatz | mittel | 2026-08-25 | **v2026.08.29** |
| BF-66 | B13 | Abschnitt „Wie die Punktzahl entsteht" mit allen acht Merkmalen | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-67 | B16 | Häuser ohne Erhebung bekommen `null` statt 0 und fallen aus dem Durchschnitt | mittel | 2026-08-25 | **v2026.08.29** |
| BF-68 | B24 | `_locale` aus dem Query gefiltert, Sprache zuletzt gemergt | hoch | 2026-08-25 | **v2026.08.29** |
| BF-69 | B24 | Elf Schlüssel ergänzt, drei Templates auf vorhandene umgestellt | mittel | 2026-08-25 | **v2026.08.29** |
| BF-70 | B24 | `lang="de"` und Herkunftshinweis auf den Freitexten | mittel | 2026-08-25 | **v2026.08.29** |
| BF-71 | B24 | `keydown.esc` schließt den Umschalter und gibt den Fokus zurück | niedrig | 2026-08-25 | **v2026.08.29** |
| BF-72 | B24 | Umschalter auch auf Mobil sichtbar | mittel | 2026-08-25 | **v2026.08.29** |
| BF-01 | B01 | „Bestätigungsmail erneut senden" war unerreichbar — Routen-Requirement ergänzt | hoch | 2026-08-23 | **v2026.08.29** |
| BF-02 | B01 | Registrierung ohne Rate Limit — Limiter `registration` (5/h) und `verify_resend` (3/h) | hoch | 2026-08-23 | **v2026.08.29** |
| BF-05 | B01, B04 | Roher Übersetzungsschlüssel — `form.password_mismatch` in allen vier `validators.*.yaml` | mittel | 2026-08-23 | **v2026.08.29** |
| BF-06 | B01 | Bestätigungstoken im Log — `doctrine`-Channel in `prod` ausgeschlossen. **Nur der prod-Weg**; der dev-Teil ist bewusst offen, siehe BF-12 | mittel | 2026-08-23 | **v2026.08.29** |
| BF-07 | B01 | Hartkodierte deutsche Meldung — `user.email_unique` statt Klartext (Enumeration bleibt offen, siehe BF-09) | mittel | 2026-08-23 | **v2026.08.29** |
| BF-08 | B01 | Mail-Locale bei asynchronem Versand — `->locale()` gesetzt (**nur B01**, B14/B15/B23 weiterhin betroffen) | mittel | 2026-08-23 | **v2026.08.29** |
| BF-16 | B02, B04 | **Rekonstruktion falsch** — B02/EC-04, B04/AK-13 und B04/FB-04 behaupteten, Sitzungen und `remember_me`-Cookies überlebten eine Passwortänderung. **Beide Specs am 2026-08-24 berichtigt**, Regressionstest angelegt | mittel | 2026-08-24 | entfällt (Dokumentation) |
| BF-13 | B02 | Anmeldung ohne Sperre — `login_throttling` mit 5 Versuchen je 15 Minuten ergänzt | **hoch** | 2026-08-24 | **v2026.08.29** |
| BF-24 | B23 | API umging die Moderation — `create()` legt jetzt einen `RestaurantSuggestion` an und antwortet mit 202; `cuisines` ohne `findOrCreateByName()`; nicht übermittelte Merkmale sind `UNKNOWN` statt `false` | **hoch** | 2026-08-24 | **v2026.08.29** |
| BF-25 | B23 | `register` unter dem schwachen Limit — eigener Limiter `api_register` (5/h) | **hoch** | 2026-08-24 | **v2026.08.29** |
| BF-26 | B23 | Formatvertrag der JWT-Antworten — `ApiAuthenticationFailureSubscriber` für alle vier Fälle des Bundles | mittel | 2026-08-24 | **v2026.08.29** |
| BF-27 | B23 | Zu lange Küchen-Angabe → 422 statt 500 | niedrig | 2026-08-24 | **v2026.08.29** |
| BF-28 | B23 | 404-Meldungen ohne interne Klassennamen | niedrig | 2026-08-24 | **v2026.08.29** |
| BF-64 | B12 | Startseite zeigte ein Restaurant statt sechs — `findTopRated()` nutzt jetzt `Paginator` mit `$fetchJoinCollection`. Der vorhandene Test prüfte `assertLessThanOrEqual` und war grün; er prüft jetzt `assertCount` | mittel | 2026-08-24 | **v2026.08.29** |
| BF-46 | B10 | Erfundene Barrierefreiheitsaussage — Texte sagen jetzt, was tatsächlich geprüft wurde, samt Radius und Herkunftshinweis; Radius 500 → 1000, damit 8 statt 3 von 11 Restaurants Haltestellen zeigen | mittel | 2026-08-24 | **v2026.08.29** |
| BF-44 | B10 | Kein Timeout — `'timeout' => 3`, `'max_duration' => 5`; Rückkehr nach 0,3 s statt >30 s | mittel | 2026-08-24 | **v2026.08.29** |
| BF-45 | B10 | API-Schlüssel im Log — Service protokolliert Klasse und Code statt der URL; `SecretMaskingProcessor` deckt Symfonys `http_client`-Kanal ab. 22 Zeilen mit `accessId=`, **0 im Klartext** | mittel | 2026-08-24 | **v2026.08.29** |
| BF-14 | B02 | Abmelden ohne CSRF — `enable_csrf` plus POST-Formular in der Kopfzeile | niedrig | 2026-08-24 | **v2026.08.29** |
| BF-19 | B04 | E-Mail-Änderung ohne erneute Bestätigung — neue Adresse wird nur noch vorgemerkt (`User::$pendingEmail`), Bestätigungslink an die neue und Warnung an die bisherige Adresse | **hoch** | 2026-08-24 | **v2026.08.29** |
| BF-20 | B04 | Passwortänderung ohne Rate Limit — Limiter `password_change` (5 je 15 Minuten), gezählt **am Konto** statt an der IP | niedrig | 2026-08-24 | **v2026.08.29** |

## Akzeptiert

Bewusst nicht behoben. Ohne Begründung und Datum ist ein Befund nicht akzeptiert,
sondern vergessen.

| ID | Feature | Befund | Grad | Begründung | Beschlossen am |
|---|---|---|---|---|---|
| BF-12 | B01 | Bestätigungstoken steht weiterhin im `dev`-Log (`doctrine.DEBUG`) | niedrig | Ein Entwicklungslog ohne SQL-Historie wäre für die Fehlersuche wertlos, und es verlässt den Rechner nicht. Der Weg, auf dem der Token in `prod` ins Hoster-Log geriet, ist mit BF-06 geschlossen. Deshalb bleibt AK-17 formal durchgefallen, ohne dass ein Befund offen ist | 2026-08-23 |
| BF-03 | B01 | Unbestätigte Konten haben vollen Zugang — kein `user_checker` | hoch | Ein `user_checker` sperrt bestehende unbestätigte Konten im Moment des Deployments aus; wie viele das auf Produktion sind, ist nicht einsehbar. Betreiberentscheidung gegen einen globalen Zwang; die Voraussetzung für eine spätere Umstellung ist mit der Reparatur von BF-01 geschaffen. Dokumentiert als OF-01 der Spec | 2026-08-23 |

## Muster

- **Eine Aussage über fremde Software gilt erst, wenn sie gegen die Software gemessen wurde
  (BF-150).** Der Entwurf zitierte sogar eine Fundstelle aus Umamis Quelltext — nur belegte das
  zitierte Beispiel (`'*/thanks'`) den Fall nicht, für den es angeführt wurde. Von dort wanderte die
  Behauptung in die Konfiguration, in einen abgehakten Aufgabenpunkt und in die Dokumentation eines
  zweiten Werkzeugs; vier Stellen, keine davon ausgeführt. Kein Unit-Test kann das finden, weil jeder
  gegen die eigene Erwartung prüft, und das Code-Review hatte keine Instanz. **Regel: Wo ein Feature
  eine Abfragesprache, ein Muster oder eine Option eines Fremddienstes festlegt, gehört ein Lauf gegen
  eine echte Instanz derselben Version in den Bau — ein Container reicht** (siehe „Die
  Produktionsumgebung ist mit einem Container nachstellbar"). Die Fundstelle im Entwurf ersetzt den Lauf
  nicht; sie sagt nur, wo man nachsehen müsste.

- **Ein Zeitlimit begrenzt die Wartezeit eines Aufrufs, nicht die Summe gleichzeitiger (BF-149).**
  Nahverkehr (`'timeout' => 3`) und Puls (`'timeout' => 5`) laufen je Anfrage einmal; der Zählweg läuft
  bei jedem Seitenaufruf, also parallel. Ein Unterbrecher, der erst nach dem ersten Fehlschlag greift,
  lässt alle Anfragen warten, die vor ihm eintreffen. **Regel: Wer einen Fremddienst aus einem Weg ruft,
  der pro Besucher läuft, prüft Zeitlimit und Unterbrecher mit gleichzeitigen Anfragen gegen einen
  hängenden — nicht nur einen abgeschalteten — Dienst.** Ein abgeschalteter antwortet sofort mit
  „Verbindung abgelehnt" und sieht harmlos aus.

- **Eine Reproduktion kann selbst falsch sein — und fällt erst nach der Reparatur auf
  (BF-122).** Die Sonde alterte nur `created_at`, während ein realer Eintrag beide
  Zeitstempel in der Vergangenheit hat. Solange beide Spalten dieselbe Frist trugen, war
  das folgenlos; nach der Umstellung war es der Unterschied zwischen „behoben" und „nicht
  behoben". **Regel: Wenn eine Reproduktion nach der Reparatur weiterhin greift, wird
  zuerst die Reproduktion geprüft — nicht sofort die Reparatur verworfen.**

- **Ein Feld mit zwei Aufgaben bricht, sobald eine davon geändert wird (BF-122).** `createdAt`
  trug die Frist des Bestätigungslinks **und** die Aufbewahrungsfrist. Die Reparatur an der
  einen hob die andere auf — und beide Prüfläufe blieben grün, weil jeder nur **eine**
  Erneuerung testet. **Regel: Wer ein Feld zurücksetzt, sucht vorher jede Abfrage, die es
  liest.** Dasselbe Muster wie `confirmedAt`, das seit BF-89 zwei Bedeutungen trug und
  deshalb `selfConfirmedAt` bekam.
- **Ein Formular ohne `action` funktioniert nur dort, wo die aktuelle Adresse zufällig die POST-Route ist — und
  jeder Test, der es von dort holt, bestätigt das (BF-151).** Dasselbe Partial auf vier Seiten, abgeschickt wurde
  in allen Prüfläufen nur von der einen, auf der es passte. Drei QA-Durchläufe von B15 prüften die Zielgruppenseiten
  per GET und hielten sie für fertig. **Regel: Wird ein Formular-Partial auf mehreren Seiten eingebunden, trägt es
  sein Ziel ausdrücklich, und der Prüflauf schickt es von jeder dieser Seiten ab.** Gefunden hat es erst ein
  Messtrichter, der dort keine Eintragung bekam — ein Weg, den niemand als Prüfung gebaut hatte.

- **Eine Reparatur wird gegen ihre Reproduktion UND gegen ihre Nachbarschaft geprüft
  (BF-117 → BF-122).** Der erste Nachweis war vollständig: Der neue Link löste ein. Was
  fehlte, war die Frage, was das Zurücksetzen sonst noch bewegt. **Regel: Nach jeder
  Reparatur einen eigenen Durchgang „was hat sich dadurch verschoben" — er findet, was
  die Reproduktion des behobenen Fehlers strukturell nicht finden kann.**
  ⚠ **Zweite Ausprägung, bei einem neuen Feature (BF-149 → BF-152, 2026-09-13):** Die Reparatur führte eine Sperre
  ein und nahm die Fehlerbehandlung der Nachbarzeilen nicht mit — jeder andere Ausfall in `UmamiForwarder` endet
  in `catch (\Throwable)`, dieser als 500er. Die neuen Prüfläufe waren gründlich (drei Fälle, zwei rote
  Gegenproben) und prüften doch nur, was die Sperre tun soll, nicht wie sie scheitert. Gefunden vom
  `code-reviewer`, nicht von der Reproduktion.

- **Die Spezifikation eines Bestandsfeatures veraltet mit jeder Reparatur, die es
  behebt (BF-126, BF-130).** Zweimal in Folge gefunden, beide Male dasselbe: Die
  Rekonstruktion stammt vom 2026-08-24, BF-36/37/38 wurden am 2026-08-29 ausgeliefert,
  und niemand hat die Spec nachgezogen. **Die Reparatur wird gebucht, die Anforderung
  nicht.** Besonders teuer im Abschnitt *Fehlbestand*: Dort stehen FB-01 und FB-03 als
  offene DSGVO-Lücken, obwohl sie seit zwei Wochen behoben sind.
  ⚠ **Auch bei einem neuen Feature, und dort strukturell (BF-147, 2026-09-13):** Die Reparatur
  widerspricht `design.md` Entscheidung 11 („nur die Restaurantliste blättert"). `sdd-build` darf den
  Entwurf nicht ändern, die QA ebenso wenig. Der einzige Weg ist eine offene Frage (OF-07), und der
  Entwurf bleibt falsch, bis der Betreiber sie entscheidet. Im Code-Review der Reparatur aufgefallen,
  nicht im Bau.
  **Regel: Wer einen Befund auf `behoben` setzt, prüft im selben Zug, welche Kriterien
  und welcher Fehlbestand ihn beschreiben.** ⚠ Und beim Prüfen: Eine Drift-Prüfung, die
  nur die AK-Tabelle liest, findet die Hälfte nicht — `spec.md` hat drei weitere
  Abschnitte, `design.md` eigene Tabellen.

- **Eine Rekonstruktion veraltet auch dort, wo niemand sie liest — und benennt dann das
  Falsche als „zentralen Befund" (BF-142).** Vierte Ausprägung nach BF-126, BF-130 und
  BF-132, diesmal bei einem Feature, das **nie** durch die QA gegangen war: Die Spec von
  B25 stammt vom 2026-08-23 und behauptet, auf dem Telefon gebe es keinen Weg zum
  Abmelden und zur Sprachwahl. Beides ist seit BF-72 bzw. seit jeher vorhanden — gemessen
  387 × 44 px und 55 × 28 px. **Regel: Vor dem ersten QA-Lauf eines Bestandsfeatures
  gehört die Spec gegen den heutigen Code gelesen, nicht gegen den vom Erfassungstag.**
  ⚠ Und die Umkehrung gilt genauso: Was die Spec als zentralen Befund führt, bestimmt,
  wo gebaut wird — eine falsche Angabe dort kostet nicht nur Genauigkeit, sondern Arbeit
  an der falschen Stelle (OF-01 von B25 fragt nach einem Menüfeld für Wege, die es gibt).

- **Ein fremder Vertragstext veraltet lautlos, und die eigene Dokumentation veraltet mit
  ihm (BF-139).** Der AV-Vertrag mit Brevo war am 2026-09-05 sorgfältig gelesen und
  Klausel für Klausel richtig wiedergegeben — nur war er am 2026-09-12 nicht mehr in
  Kraft. Der Anbieter benachrichtigt über neue **Unterauftragsverarbeiter** (Ziff. 7.3),
  nicht über eine neue Fassung des Vertrags selbst. **Regel: Wer einen fremden Vertrag
  dokumentiert, notiert das Abrufdatum, legt eine Kopie daneben und setzt eine
  Wiedervorlage** — hier DS-01e, jährlich. ⚠ Und beim Nachziehen nicht still
  überschreiben: Eine Tabelle „alte Klausel gegen neue" zeigt, was sich geändert hat, und
  genau das braucht jemand, der eine ältere Auskunft verantworten muss.

- **Eine eingesetzte Störung muss nachweisen, dass sie ankommt (BF-131, BF-135).**
  `testMailerFailureShowsWarningAndStillRedirects` setzte über
  `$client->getContainer()->set(MailerInterface::class, …)` einen Mailer, der bei jedem
  Aufruf wirft — und **zwei Mails gingen trotzdem hinaus**. Der Grund: Der Mailer ist ein
  **Methodenargument** des Controllers und wird aus dessen Dienst-Locator geholt, den das
  Überschreiben im Test-Behälter nicht erreicht. Der Lauf prüfte am Ende nur, dass eine
  ungestörte Registrierung weiterleitet, und trug zwei Wochen den Namen einer Prüfung,
  die er nicht war. **Regel: Wer eine Störung einsetzt, prüft zuerst die Störung** — hier
  über den Zähler der versandten Mails. Was tatsächlich trägt, ist ein unerreichbarer
  Port (`MAILER_DSN=smtp://127.0.0.1:1`), und der Wert gehört im `tearDown()`
  **zurückgestellt**, nicht gelöscht: `.env.test` füllt `$_ENV` einmalig beim Bootstrap,
  ein `unset()` lässt `%env()%` undefiniert und der nächste Aufruf endet mit 500.

- **Ein Element, das schrumpfen darf, versteckt den Überlauf, den es verursacht
  (BF-80).** Der Kopfzeilen-Befund war mit 36 px (abgemeldet) und 81 px (angemeldet)
  erfasst. Nach `shrink-0` am Logo zeigte dieselbe Messung in **Französisch 149 px** —
  vorher hatte das Logo nachgegeben und war auf 30 px zusammengedrückt, ein Viertel
  seiner Breite. **Regel: Bei einem Layoutbefund erst festnageln, was nicht nachgeben
  darf, dann messen** — und **in allen vier Sprachen**, denn Französisch braucht
  regelmässig 15–20 % mehr Platz. Dieselbe Falle wie bei den Wortgrenzen der
  Pressetexte (Feature 05).

- **Ein Prüflauf, der Quelltext als Zeichenkette durchsucht, prüft den Kommentar mit
  (BF-125).** `testAk23…` suchte `limiter.partner_waitlist` im `OrganisationController`.
  Nach der Reparatur BF-38 steht der Ausdruck dort nur noch in dem Kommentar, der die
  Reparatur **erklärt** — der Test blieb grün und behauptete das Gegenteil des
  tatsächlichen Verhaltens. **Regel: Ein Verhalten wird am Verhalten geprüft, nicht am
  Quelltext.** Muss es doch der Quelltext sein, dann über den Container
  (`$container->get('limiter.…')`) oder über Reflection, nie über `file_get_contents()`.
  ⚠ Dies ist die dritte Ausprägung desselben Musters, direkt darunter — und die
  heimtückischste: Die beiden anderen prüften zu schwach, dieser prüft das Gegenteil.

- **Ein Test, der die Zusicherung schwächer fasst als das Kriterium, ist ein grüner Test
  über einem Fehler (BF-64, BF-117).** Zweimal derselbe Bau: `findTopRated()` wurde mit
  `assertLessThanOrEqual($limit, …)` geprüft, während die Seite eine Karte zeigte; der
  neu ausgestellte Bestätigungstoken wurde auf *Änderung* geprüft, während er nicht
  *trug*. **Regel: Prüfe die Wirkung, nicht das Anzeichen.** Bei einer Begrenzung
  `assertCount(min(...))`, bei einem neu ausgestellten Zugang den Aufruf des Zugangs.
- **Ein Zweig, der vor dem `consume()` zurückkehrt, hebt den Deckel auf (BF-118).** Die
  Konvention „erst verbrauchen, wenn die Handlung stattfindet" (BF-11) hat eine Kehrseite,
  die im Projekt bisher nicht stand: **Jeder `return` zwischen `isAllowed()` und
  `consume()` ist ein ungedeckelter Weg.** Wer einen Frühausstieg einbaut, der eine Mail
  auslöst, zieht den Verbrauch mit. Der Fehler ist unsichtbar, weil der Limiter
  konfiguriert, verdrahtet und von `LimiterCoverageTest` bestätigt ist — er greift nur
  auf einem Weg nicht.
- **Die Eingabeprüfung muss gegen dieselbe Norm prüfen wie die nächste Schicht (BF-119).**
  Vierter Fall des Musters „Die Prüfung gehört dorthin, wo der Wert hereinkommt"
  (BF-27, BF-51, BF-62). Neu ist die Ursache: Nicht eine **fehlende** Prüfung, sondern
  eine, die **großzügiger** ist als der Empfänger. `Email` im HTML5-Default lässt durch,
  was `Mime\Address` (RFC 2822) ablehnt. Betrifft alle vier Formulare des Projekts.
- **Ein Helfer, der für zwei Fälle passt, passt beim dritten womöglich nicht (BF-120).**
  `getDisplayName()` liefert bei B14/B15 einen Organisationsnamen und bei der
  App-Warteliste ein Plattform-Label — dieselbe Zeile im Marketing-Register schickt beides
  als `ORGANISATION` an Brevo. Der Entwurf hatte den Fall vorhergesehen und ausdrücklich
  ausgeschlossen; übersehen wurde die **indirekte** Übertragung über einen geteilten
  Aufruf. **Regel: Wer eine dritte Quelle an einen geteilten Weg hängt, prüft den
  tatsächlichen Payload — nicht den Aufruf, der ihn baut.**


- **Die Produktionsumgebung ist mit einem Container nachstellbar — und das ist billig.**
  BF-116 galt als „lokal nicht reproduzierbar", bis ein einziger Befehl den Beweis lieferte:
  `docker run --rm -v "$PWD":/app -w /app php:8.4-cli php <skript>`. Ohne Extensions, ohne
  Datenbank, in unter einer Minute — es genügte, Doctrines `AttributeDriver` die Metadaten
  aufbauen zu lassen. **Regel: Bevor ein Befund als „nur auf Produktion" abgelegt wird,
  wird die Laufzeitumgebung im Container nachgestellt.** Für die PHP-Version reicht das
  offizielle Image; für Apache/`mod_dir` (BF-100) wäre es `php:8.4-apache`.
- **Lokal ≠ Produktion, dritter Fall (BF-100, BF-116).** Drei Abweichungen sind belegt und
  jede hat einen Deploy gekostet: **Apache/`mod_dir`** (BF-100, der Entwicklungsserver hat
  es nicht), **MySQL 8 gegen MariaDB 10.5** (dokumentiert, noch ohne Vorfall) und seit
  BF-116 die **PHP-Version** — lokal 8.5.2, auf Produktion 8.4.22. Alle drei sind vor dem
  Deploy unsichtbar und werden von keinem Prüflauf erfasst: `doctrine:schema:validate` und
  `cache:warmup --env=prod` liefen lokal grün. **Regel: Was aus der Laufzeitumgebung kommt
  — Webserver, Datenbank, Sprachversion —, ist lokal nicht geprüft, sondern nur nicht
  aufgefallen.** Ein Prüflauf gegen PHP 8.4 in der CI wäre die naheliegende Absicherung.
- **Ein Zustand, den der Kalender erst später herstellt, wird heute nicht geprüft (BF-114, BF-115).**
  Beide Befunde sitzen im selben Zweig — dem zugeklappten Jahr — und beide waren
  unsichtbar, solange die Registry nur ein Jahr führte. Sie kamen erst zum Vorschein, als
  eine **Vollbetriebs-Probe** den Bestand wachsen ließ (zwei Jahre, ein Release mehr, ein
  zurückgestellter Punkt mehr). Drei QA-Durchläufe gegen den eingefrorenen Stand hatten
  sie nicht gefunden. **Regel: Wer einen Zweig baut, der von Datum oder Bestand abhängt,
  stellt diesen Zustand einmal her — im Test oder von Hand.** Verwandt mit der Lehre aus
  Feature `05`, dort formuliert als *den Zustand herstellen, statt auf ihn zu warten*.
- **Feste Zahlen in Prüfläufen, die vom wachsenden Bestand abhängen (BF-112, BF-113, BF-114).**
  Dreimal dieselbe Bauart in einem Feature: `assertCount(10, …)` für die Changelog-Einträge,
  `assertCount(8, …)` für die zurückgestellten Punkte, feste `<details>`-Zahlen für die
  Jahresgruppierung. Alle drei sind grün, solange sich nichts ändert — und werden rot,
  sobald der Regelbetrieb genau das tut, wofür das Feature gebaut wurde: ein Release
  hinzufügen, ein Vorhaben zurückstufen, ein Jahr weiterrücken. **Ein Prüflauf, der bei
  jedem korrekten Vorgang anschlägt, wird nach dem dritten Mal ignoriert** — dann fehlt
  die Absicherung, für die er gebaut wurde. **Regel: Wer eine Liste zählt, leitet die
  erwartete Zahl aus der Quelle ab und führt den Lauf einmal mit einem zusätzlichen
  Eintrag aus.** BF-112 wurde behoben, ohne die Lehre auf die Nachbarschaft anzuwenden —
  dasselbe Versäumnis wie bei BF-107 → BF-108.
- **Ein Titel neben einem `shrink-0`-Abzeichen schrumpft auf wenige Pixel (BF-107, BF-108).**
  Zweimal dieselbe Bauart, zweimal derselbe Fehler: Ein `<h3>`/`<h4>` und ein Statusabzeichen
  in einem `flex … justify-between`, das Abzeichen mit `shrink-0`, die Überschrift ohne
  `min-w-0`. Sobald der Container schmal wird, gewinnt das Abzeichen den ganzen Platz und
  der Titel wird zur Buchstabenkolonne. Feature `06` brauchte dafür zwei Anläufe und endete
  bei `basis-full min-w-0 sm:basis-auto sm:flex-1`; Feature `07` baute die Karte neu und
  begann von vorn. **Die Klassenkette gehört ins Design-System**, nicht in ein Template —
  sonst wiederholt sie sich bei der nächsten Karte ein drittes Mal.
- **Gemessen wird an den Enden, kaputt ist die Mitte (BF-80, BF-108).** Alle Kriterien der
  Features `02`, `03`, `05` und `07` nennen 320 px, die QA von `03` zusätzlich 375 px. Beide
  Befunde sitzen bei **768 px** — dem Umbruchpunkt, ab dem mehrspaltige Layouts und die
  Desktop-Navigation greifen. BF-80 wurde deshalb zweimal übersehen, BF-108 einmal.
  **768 px gehört als dritte Messbreite in jedes Kriterium zur Darstellung.**
**Nutzertext ohne Umbruchregel sprengt schmale Ansichten — zum zweiten Mal.**
*(2026-08-30, Feature 06, BF-106; zuvor Feature 03, BF-82)* Beide Male dieselbe Ursache:
Ein Wort ohne Trennstelle, länger als die Kartenbreite, und die Seite scrollt waagerecht.
In `03` kam der Text aus einer gepflegten Anbieterliste und der Befund blieb *niedrig*; in
`06` kommt er aus einem Formular, das 120 Zeichen erlaubt — **jeder Besucher sieht die
Folge, und jeder angemeldete Nutzer kann sie auslösen.** ⚠ Beim zweiten Auftreten ist die
Einzelreparatur die falsche Antwort: Es fehlt eine Regel im Design-System, dass **jedes
Element mit Nutzertext** eine Umbruchregel trägt (`overflow-wrap: anywhere`). Sonst
wiederholt es sich beim dritten Feature, das Freitext anzeigt — und das sind Bewertungen,
Kommentare und Korrekturhinweise, alle drei bereits auf der Roadmap.

**Ein Prüflauf, der Klassen liest, sieht keine gerenderten Maße.**
*(2026-08-30, Feature 06, BF-107)* `BoardTargetSizeTest` entstand als Regressionsschutz für
eine zu kleine Zielgröße und prüft, dass `min-h-[44px]` im Markup steht. Genau das stand
dort — und trotzdem war das Ziel 36 px **breit**. Ein Markup-Test kann eine Klasse
bestätigen, nicht ihre Wirkung. Wo ein Kriterium eine **Größe** nennt, braucht es eine
Messung im Browser; der Markup-Test ist die billigere Ergänzung, nicht der Ersatz.

**Tailwind scannt Templates, nicht nur `assets/` — ein Twig-Template ist eine
Asset-Änderung.** *(2026-08-30, Feature 06, BF-105)* Die Projektregel lautet bisher
„Änderung unter `assets/` → `npm run build` und `public/build` mitcommitten". Feature 06
fasste `assets/` **nicht** an, legte aber neun Templates mit neuen Utility-Klassen an.
`assets/styles/app.css` führt `@source "../../templates"` — die Klassen entstehen also
nur mit einem Bau. Aufgabenplan und Abschlussbericht schlossen aus „keine
`assets/`-Änderung" auf „kein Bau nötig", und der committete CSS-Stand blieb ohne
`line-clamp-3`. ⚠ Gemerkt hätte es niemand vor dem Deploy: Die Testsuite ist grün, weil
sie kein CSS auswertet — erst `verify-assets` hätte blockiert, und erst eine Messung im
Browser zeigt die Folge. **Die Regel gehört auf „Änderung unter `assets/` oder
`templates/`" erweitert.**

**Ein Prüflauf, der Schlüssel nur als Literal sucht, sieht die zusammengesetzten nicht.**
*(2026-08-30, Feature 06)* `CatalogueCompletenessTest` scannt `|trans` in Templates und
`src/Form/`. Ein Übersetzungsschlüssel, der in PHP entsteht — hier
`BoardIdeaStatus::transKey()` → `'board.status_' . $this->value` —, kommt in keinem der
beiden Töpfe vor. Beim Bau wich er von den Katalogen ab, die Suite blieb grün, und der
rohe Schlüsselname stand auf der Seite. Gefunden hat es erst ein Test, der die
**gerenderte Seite** liest (`BoardLocaleTest`). ⚠ Das gilt über Feature 06 hinaus:
Überall dort, wo ein Enum oder ein Dienst seinen Schlüssel zusammensetzt, prüft der
Katalogtest nichts. Die Gegenmaßnahme ist nicht ein größerer Scanner, sondern ein Abruf
der fertigen Seite.

**Konfiguration und Katalogeinträge gehören in dieselbe Ebene wie das Artefakt, das sie
benutzt.** *(2026-08-30, Feature 06 — dreimal in einem Bau)* Ein Limiter ohne Aufrufer
färbt `LimiterCoverageTest` rot; eine Mailvorlage ohne ihre Schlüssel und ein Formular
ohne seine Beschriftungen färben `CatalogueCompletenessTest` rot. Ein Aufgabenplan, der
sie in eine frühere Ebene legt, lässt die Suite zwangsläufig über mehrere Ebenen rot —
und damit fällt genau die Regel aus, die den Bau trägt („rot heißt anhalten"). CLAUDE.md
sagt es für den Limiter bereits: „legt den Limiter **im selben Commit** an."

**Ein Ast, der nie ausgeführt wird, ist keine Abdeckung — er sieht nur so aus.**
*(2026-08-30, Feature 05, BF-97)* Ein Prüflauf verzweigte an einer Vorbedingung
(`PressPackage::exists()`) und prüfte in der vorhandenen Umgebung ausschließlich den
Ersatzzweig. Der Regelfall des Features lag damit in keinem einzigen Test, und ein
Fehler darin — die Seite antwortet mit 500 — wurde erst sichtbar, als die QA den Zustand
**herstellte** statt ihn abzuwarten. Wo eine Vorbedingung offen ist, wird der Zustand
dahinter simuliert; sonst wächst die grüne Suite genau um die Stellen, die niemand
gesehen hat.



Was in mehr als einem Feature auftritt — der Grund, warum diese Liste existiert.

- **Eine Reparatur öffnet einen Pfad, den vorher eine Abbruchbedingung verdeckte
  (BF-89 → BF-91).** Dritte Runde an derselben Stelle, und diesmal war die Ursache eine
  andere: `WaitlistConfirmationService::confirm()` stieg bei einem verwaltungsseitig
  bestätigten Eintrag früher mit `RESULT_ALREADY` aus. Die Reparatur nahm diese
  Abbruchbedingung weg — richtigerweise, denn sie verschluckte echte Bestätigungen —, und
  damit wurde `Entity::confirm()` in einer Lage erreicht, für die es nie geschrieben war:
  Es setzt auch den **Status**, und ein gewonnener Kunde fiel auf „bestätigt" zurück.
  **Folgerung: Wer eine Abbruchbedingung entfernt, prüft, was dahinter liegt.** Der
  entfernte `return` war die einzige Stelle, die einen ganzen Codepfad unerreichbar hielt
  — sein Wegfall ist kein kleinerer Eingriff als eine neue Verzweigung.
- **Eine Reparatur an der Reihenfolge verschiebt einen Fehler, der aus einer
  Zweideutigkeit stammt (BF-83 → BF-89).** `confirmedAt` bedeutet zweierlei — eingelöster
  Double-Opt-In und Verwaltungs-Backfill. Die erste Reparatur zog die Prüfung **vor** den
  Backfill; damit war der erste Statuswechsel sauber und der zweite wieder falsch, weil
  er das nachgesetzte Feld vorfindet. **Folgerung: Wenn ein Feld zwei Bedeutungen trägt,
  ist jede Reparatur an der Reihenfolge ein Aufschub.** Die Frage vor der nächsten
  Reparatur lautet nicht „wann prüfen wir", sondern „woran unterscheiden wir die beiden
  Fälle". Verwandt mit dem Muster darunter: Auch hier stimmte der Kommentar — er
  beschrieb nur den Fall, den der Autor vor Augen hatte.
- **Ein Kommentar, der eine Zusage macht, die der Code nicht hält (BF-46 behoben, BF-86
  offen).** Zweimal derselbe Mechanismus in einem Projekt, das bewusst dicht und
  begründend kommentiert: Bei BF-46 sagte der **Nutzertext** „barrierefreie Haltestellen",
  während die Schnittstelle kein solches Merkmal kennt. Bei BF-86 sagt der
  **Codekommentar**, der Sync-Lauf greife fehlgeschlagene Zeilen „über ihren
  Versuchszähler wieder auf, nicht über den Zustand" — die Abfrage tut genau das nicht.
  Beide Male hat der Kommentar die Prüfung *ersetzt* statt sie zu leiten: Wer ihn liest,
  hält die Sache für geklärt und sieht nicht mehr nach. **Folgerung für künftige QA: Ein
  ⚠-Kommentar, der ein Verhalten zusichert, ist ein Prüfauftrag, kein Nachweis.**
- **Ein geteilter Schlüssel löst einen Fall auf der Schreibseite und bricht ihn auf der
  Löschseite (BF-84, offen).** `marketing_contact` hat bewusst *eine Zeile je Adresse* —
  das löst EC-01 („eine Adresse, ein Kontakt") beim Eintragen sauber und auf
  Datenbankebene. Beim **Austragen** kippt dieselbe Entscheidung: Der Löschauftrag kennt
  die auslösende Quelle nicht und nimmt die anderen mit. Bisher einmalig; die Zeile steht
  hier, weil dieselbe Frage bei jedem künftigen quellenübergreifenden Schlüssel wieder
  auftaucht — die Entwurfsprüfung sollte zu jedem geteilten Schlüssel ausdrücklich fragen,
  was beim **Entfernen** einer von mehreren Quellen geschieht.

- **Fehlende Eingabeprüfung endet im 500er (BF-27 behoben, BF-51, BF-62, BF-68 offen).**
  Viermal dasselbe: Ein Wert, den niemand geprüft hat, fällt in die nächste Schicht und
  kommt dort als Serverfehler heraus — zu lange Küchen-Angabe über die API (BF-27), leeres
  Pflichtfeld (BF-51), zu langer Küchenname (BF-62), Sprachcode aus dem Query (BF-68).
  Der vierte Fall ist der schwerste, weil er **ohne Anmeldung und ohne POST** auslösbar ist
  und jede öffentliche Seite trifft. Gemeinsam ist allen, dass die Prüfung an der Stelle
  fehlt, an der der Wert **hereinkommt** — nicht dort, wo er verbraucht wird.
- **Eine Rekonstruktion kann auch richtig sein und trotzdem in die Irre führen (BF-68).**
  B24/AK-12 beschreibt die Merge-Reihenfolge im Sprachumschalter **korrekt** — und nennt
  sie „harmlos, aber leicht zu beheben". Gemessen ist sie ein Open Redirect plus ein
  auslösbarer 500er auf zehn Seiten. Das Verhalten war richtig erfasst, der Schaden nicht.
  Ergänzung zu BF-16: Eine `spec.md` eines Bestandsfeatures ist nicht nur dort angreifbar,
  wo sie das Verhalten falsch beschreibt, sondern auch dort, wo sie es bewertet.
- **Fehlende Rate Limits im Browser-Weg (BF-02 behoben, BF-13 offen).** Die Anwendung
  drosselt die API (`api_login`: 5/min, zweimal belegt: ab dem sechsten Versuch 429), aber
  kaum einen Web-Endpunkt. Für die Registrierung ist es behoben, für die **Anmeldung**
  nicht — dort wurden 20 Fehlversuche gegen das Admin-Konto alle angenommen. Offen bleiben
  nach der Rückerfassung Passwortwechsel, Passkey-Challenge, Vorschläge und
  Datensatz-Download (M-01 in `fehlbestand-uebersicht.md`). Das Muster hat sich damit
  bestätigt: Geschützt ist der Weg, den eine App nimmt; ungeschützt der, den ein Browser
  nimmt. Mit BF-18 kommt ein dritter Fall dazu: die sprachfreien Passkey-Endpunkte fallen
  weder unter die Web- noch unter die API-Limiter.
- **Eine Rekonstruktion kann falsch sein (BF-16).** B02/EC-04 und zwei Stellen in B04
  behaupteten, eine Passwortänderung lasse fremde Sitzungen unberührt — geschlossen aus
  dem Projektcode, der tatsächlich nichts dergleichen tut. Gemessen erledigt Symfony es
  selbst. Bei jedem weiteren Feature gilt: Was das Framework leistet, steht nicht im
  Projektcode und lässt sich nur durch Ausführen feststellen.
- **Ein Übersetzungsschlüssel, zwei Features (BF-05, behoben).** Derselbe Griff in die
  falsche Übersetzungsdomäne betraf Registrierung und Passwortwechsel. Die Reparatur wirkt
  für beide; bei der QA von B04 ist nur noch zu bestätigen.
- **Locale geht bei asynchroner Verarbeitung verloren (BF-08 behoben, BF-10 offen).**
  Betrifft alle vier Mailwege des Projekts. In B01 mit `->locale()` behoben — derselbe
  Einzeiler steht für B14, B15 und B23 noch aus. Auf Produktion derzeit unwirksam, weil dort
  synchron versandt wird; kippt, sobald ein Messenger-Worker eingeführt wird. Genau das ist
  für die Monats-Snapshots vorgesehen (B18/AK-17).
- **Kontingent für ungültige Eingaben (BF-11).** Beide Rate-Limiter des Projekts —
  der bestehende in `PartnerController` und der neue in `RegistrationController` —
  konsumieren vor der Gültigkeitsprüfung. Wer sich vertippt, verbraucht dasselbe
  Kontingent wie ein Angreifer. Das ist keine Nachlässigkeit der Reparatur, sondern
  ein Muster, das sie übernommen hat; bei der QA von B14/B15 mitzuprüfen.
- **Betroffenenrechte (BF-04, jetzt Feature `01`).** Kein Löschweg, kein Export, kein
  Widerruf. Die Rückerfassung nennt dieselbe Lücke für die Wartelisten (B14, B15, B22).
  Es ist die einzige Befundgruppe mit rechtlicher Frist — und die technischen
  Voraussetzungen (Kaskaden, `SET NULL`, Dateilöschung) sind bereits vollständig da.
  **Seit 2026-08-23 als eigenes Feature geführt**, weil ein Befund, der drei Features
  betrifft und keine Reparatur ist, sonst eines davon dauerhaft blockiert.

## Reihenfolgeabhängigkeiten

Nicht jeder Befund lässt sich einzeln beheben:

- **BF-02 vor BF-01** — die Reparatur der Routenkollision öffnet sonst einen
  ungedrosselten Mailversand auf ein fremdes Postfach.
- **BF-01 vor BF-03** — ein `user_checker` sperrt alle bestehenden unbestätigten Konten
  aus, solange sie keine neue Bestätigungsmail anfordern können.
