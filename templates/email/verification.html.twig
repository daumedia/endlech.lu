{% extends 'email/base.html.twig' %}

{% block title %}{{ 'email.verify_title'|trans }}{% endblock %}

{% block body %}
    <h2 style="margin: 0 0 16px; color: #111827; font-size: 20px; font-weight: 700;">
        {{ 'email.verify_greeting'|trans({'%name%': user.name}) }}
    </h2>

    <p style="margin: 0 0 24px; color: #6b7280; font-size: 14px; line-height: 1.6;">
        {{ 'email.verify_text'|trans }}
    </p>

    <table role="presentation" style="width: 100%; border: 0;">
        <tr>
            <td style="text-align: center; padding: 8px 0 24px;">
                <a href="{{ verifyUrl }}" style="display: inline-block; background-color: #9333ea; color: #ffffff; padding: 14px 32px; border-radius: 8px; text-decoration: none; font-weight: 700; font-size: 14px;">
                    {{ 'email.verify_button'|trans }}
                </a>
            </td>
        </tr>
    </table>

    <p style="margin: 0 0 8px; color: #9ca3af; font-size: 12px;">
        {{ 'email.verify_expiry'|trans }}
    </p>

    <p style="margin: 0; color: #9ca3af; font-size: 12px;">
        {{ 'email.verify_fallback'|trans }}<br>
        <a href="{{ verifyUrl }}" style="color: #9333ea; word-break: break-all;">{{ verifyUrl }}</a>
    </p>
{% endblock %}
