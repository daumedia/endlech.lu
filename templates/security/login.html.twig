{% extends 'base.html.twig' %}

{% block title %}{{ 'login.title'|trans }}{% endblock %}

{% block body %}
<div class="min-h-[calc(100vh-5rem)] flex items-center justify-center py-12 px-4">
    <div class="w-full max-w-md">
        <div class="bg-white rounded-2xl shadow-lg border border-gray-200 p-8">

            {# Titel #}
            <div class="text-center mb-8">
                <h1 class="text-2xl font-bold text-gray-900 mb-2">{{ 'login.heading'|trans }}</h1>
                <p class="text-gray-500 text-sm">{{ 'login.subtitle'|trans }}</p>
            </div>

            {% include 'partials/_flash_messages.html.twig' %}

            {% if error %}
                <div class="bg-red-50 border-l-4 border-red-400 text-red-800 px-4 py-3 rounded-r-lg mb-4" role="alert">
                    <p class="text-sm font-medium">{{ error.messageKey|trans(error.messageData, 'security') }}</p>
                </div>
            {% endif %}

            {# Passkey zuerst – auch in der Tab-Reihenfolge, die der sichtbaren folgen muss. #}
            {% include 'partials/_passkey_login.html.twig' %}

            <form method="post" class="space-y-5">

                {# E-Mail #}
                <div>
                    <label for="inputEmail" class="block text-sm font-semibold text-gray-700 mb-1">{{ 'login.email'|trans }}</label>
                    <input type="email" id="inputEmail" name="_username" value="{{ last_username }}" required autofocus autocomplete="email" placeholder="{{ 'login.email_placeholder'|trans }}" class="w-full bg-gray-50 border border-gray-300 rounded-lg px-4 py-2.5 text-sm focus:ring-2 focus:ring-purple-500 focus:border-purple-500 focus:outline-none transition">
                </div>

                {# Passwort #}
                <div>
                    <label for="inputPassword" class="block text-sm font-semibold text-gray-700 mb-1">{{ 'login.password'|trans }}</label>
                    <input type="password" id="inputPassword" name="_password" required autocomplete="current-password" placeholder="{{ 'login.password_placeholder'|trans }}" class="w-full bg-gray-50 border border-gray-300 rounded-lg px-4 py-2.5 text-sm focus:ring-2 focus:ring-purple-500 focus:border-purple-500 focus:outline-none transition">
                </div>

                {# Angemeldet bleiben #}
                <div class="flex items-center">
                    <input type="checkbox" id="rememberMe" name="_remember_me" class="rounded text-purple-600 focus:ring-purple-500 border-gray-300">
                    <label for="rememberMe" class="ml-2 text-sm text-gray-600">{{ 'login.remember_me'|trans }}</label>
                </div>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                {# Absenden #}
                <button type="submit" class="w-full bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 rounded-lg shadow-md hover:shadow-lg transition transform hover:-translate-y-0.5">
                    {{ 'login.submit'|trans }}
                </button>
            </form>

            {# Link zu Registrierung #}
            <p class="text-center text-sm text-gray-500 mt-6">
                {{ 'login.no_account'|trans }}
                <a href="{{ path('app_register') }}" class="text-purple-600 font-semibold hover:text-purple-800 transition">{{ 'login.register_link'|trans }}</a>
            </p>
        </div>
    </div>
</div>
{% endblock %}
